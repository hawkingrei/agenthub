# TODO

- [ ] Verify Bazel-first Rust crate decomposition: `agenthub-logging`, `agenthub-config`, and `agenthub-db` have been successfully extracted from `src/` to standalone workspace crates. The original modules in `src/` have been removed and replaced with re-exports in `src/lib.rs`. Ensure local Bazel builds and cargo test suite continue to pass cleanly in CI; record push + PR CI run IDs after merge (see `docs/journal/2026-03-15-deep-modularization-crates.md`).

- [ ] Implement P2P remote node and remote ACP execution pre-requisites:
    - [ ] Refactor `spawn_acp_session` to use generic `AsyncRead`/`AsyncWrite` instead of `ChildStdin`/`ChildStdout` to support remote/tunneled streams.
    - [ ] Introduce `AgentExecutor` trait and move current subprocess spawning into `LocalExecutor` implementation to decouple agent management from local process execution.
    - [ ] Add `target_node_id` to `AgentConfig` and `AgentRecord` to support directing agents to specific P2P nodes.
    - [ ] Add P2P/Cluster configuration section to `agenthub-config` for node IDs, listen addresses, and bootstrap peers.
    - [ ] Enhance `ActorMessageTransport` metadata to propagate peer/routing info required for P2P execution coordination.

- [ ] Verify SSE live-output resiliency after transport batching + independent active-agent catch-up polling: high-frequency Codex ACP output should reduce SSE message pressure, single-event and batched SSE payloads should both render correctly, and active-agent `/events` replay should continue recovering output after SSE reconnect/stall; record local validation notes plus push + PR CI run IDs after merge (see `docs/journal/2026-03-14-sse-output-batching-and-poll-watchdog.md`).
- [ ] Verify refreshed `agenthub-codex-acp` runtime behavior after dependency upgrade to codex rev `ab7a5b87a9e455235f65637f1675c03051481c87`, `agent-client-protocol = 0.10.2`, and `quinn-proto = 0.11.14`: cover auth/login, session load/close, MCP permission prompts, and prompt/replay flows; record local runtime notes plus push/PR CI run IDs (see `docs/journal/2026-03-14-codex-acp-dependency-refresh.md`).
- [ ] Verify `/teams` Mantine control migration on deployed pages after login: sidebar scope switch + team filter, task status/new-task/compile-preview inputs, and run status filter should preserve existing behavior under the refreshed Mantine + Tailwind shell; record Chrome DevTools MCP notes for both unauthenticated redirect baseline and authenticated workbench regression after deploy (see `docs/journal/2026-03-14-teams-mantine-control-migration.md`).
- [ ] Verify deployed `Start Team` repairs legacy worker runtime records by recovering `worktree_repo` plus safe-path-aligned worker roots, and returns explicit client errors for unrecoverable worker runtime configs instead of generic internal errors (see `docs/journal/2026-03-11-team-start-safe-path-repair.md`).
- [ ] Verify canonical Team shared-thread agent replies after server-side persistence: mailbox `chat_message` replies addressed to the human actor should appear in `/teams` as stable `team_conversation_messages` in the shared `all` thread, without frontend mailbox merge; confirm directed and broadcast routing behavior, then record push + PR CI run IDs (see `docs/journal/2026-03-13-team-shared-thread-canonical-replies.md`).
- [ ] Verify merged `team_members` Team context tool on actor MCP/CLI sessions: runtime summary (`status`, `online_count`, `member_count`), live roster/card data, and optional run overlay should all come from the single tool without requiring a second Team runtime query path; record local validation notes and push + PR CI run IDs after merge (see `docs/journal/2026-03-10-team-members-runtime-summary.md`).
- [ ] Verify Team persistent member-runtime rollout after phase-1 protocol de-runification: `create_team` eager member startup, explicit `start_team` / `stop_team`, orchestrator dispatch onto already-running member sessions, and removal of the legacy fixed-run compatibility alias should all remain stable in local validation plus push/PR CI; record migration notes and validation evidence after merge (see `docs/journal/2026-03-09-team-member-runtime-model-reset.md`, `docs/journal/2026-03-09-team-actor-runtime-protocol-derunify.md`, `docs/journal/2026-03-14-team-runtime-legacy-run-alias-removal.md`).
- [ ] Verify `/teams` runtime controls on deployed domain: header `Start Team` / `Stop Team`, runtime badge/notice copy, and `Runs` panel `Start Run` copy align with the Team member-runtime model; record Chrome MCP notes after deploy (see `docs/journal/2026-03-09-teams-runtime-controls.md`).
- [ ] Verify Team step runtime-handle naming rollout after introducing `runtime_handle_id` as the primary DB/domain/API field while preserving legacy `remote_task_id` compatibility across HTTP/proto/web clients; record local validation notes plus push/PR CI run IDs after merge (see `docs/journal/2026-03-09-team-runtime-handle-naming.md`, `docs/journal/2026-03-14-team-runtime-handle-id-compat-rollout.md`).
- [ ] Verify Team lifecycle end-to-end after merge: `create_team` auto-starts leader + workers, manual `start_team` / `stop_team` behaves correctly, and `/teams` exposes runtime state/controls without reintroducing run-owned session assumptions (see `docs/journal/2026-03-09-team-member-runtime-model-reset.md` and `docs/journal/2026-03-09-team-actor-runtime-protocol-derunify.md`).
- [ ] Verify runtime `team_members` roster query is available to actor MCP/CLI sessions, returns live member display names + card descriptions + step/session status, and remains correct after future team member add/remove operations; record push + PR CI run IDs after merge (see `docs/journal/2026-03-09-team-runtime-roster-query.md`).
- [ ] Verify `/teams` first-class `Conversation / Tasks / Mailbox` workflow on deployed pages: `Tasks` list/create/compile-preview/create-run, shared-thread `seen by N agents` expansion, member-scoped mailbox focus from the left rail, and visible `@agent_name` mention rendering while preserving canonical routing; record Chrome MCP notes plus push + PR CI run IDs after merge (see `docs/journal/2026-03-08-teams-conversation-tasks-mailbox-workflow.md`).
- [ ] Verify browser-local `Developer Mode` stays scoped to `Agents + Teams` (not `Admin`), defaults to `on` in dev/test and `off` in release bundles, and toggles the intended technical surfaces on deployed pages with live agent/team data; record Chrome MCP notes plus push + PR CI run IDs after merge (see `docs/journal/2026-03-06-teams-subject-rail-layout.md`).
- [ ] Verify Codex concurrent ACP mutation barriers prevent queued session mutations (`SetMode`/`SetModel`/`SetConfig`) from being starved by a steady prompt stream, while preserving concurrent prompt delivery before the barrier is introduced; record local validation notes and push + PR CI run IDs after merge (see `docs/journal/2026-03-06-codex-acp-concurrent-prompt-delivery.md`).
- [ ] Verify Codex ACP default `gpt-5.4` flow stays HTTP-only after disabling implicit responses websocket support in ACP config normalization, and confirm explicit websocket features still preserve ws transport when intentionally enabled; record local validation notes and any push + PR CI run IDs after merge (see `docs/journal/2026-03-06-codex-acp-disable-implicit-responses-websocket.md`).
- [ ] Verify Codex ACP concurrent prompt delivery reaches the runtime while an earlier turn is still working, while Gemini/Kimi remain strict FIFO and ACP session config mutations (`mode`/`model`/`config`) stay serialized behind active prompts; record push + PR CI run IDs after merge (see `docs/journal/2026-03-06-codex-acp-concurrent-prompt-delivery.md`).
- [ ] Implement `/teams` layout-only refresh using Slock as a composition/typography reference while preserving current project code style and current palette: keep scope limited to Team workbench files (`team_page.tsx`, `team_sidebar.tsx`, Team-local panel/layout tokens as needed), do not change `Agents` or auth pages, avoid global shell/mobile overflow rewrites, and include Chrome DevTools MCP before/after validation once MCP transport recovers; record push + PR CI run IDs after merge (see `docs/journal/2026-03-06-teams-slock-layout-direction-reset.md`, `docs/journal/2026-03-06-teams-subject-rail-layout.md`).
- [ ] Verify Team MCP enforcement profile is runtime-effective: Team leader/worker sessions fail-fast when mailbox MCP is missing, Team turn loop remains inbox-first (`inbox -> process -> ack -> send/report`), and non-Team sessions keep non-required behavior; record push + PR CI run IDs after merge (see `docs/features/team-mcp-enforcement.md`, `docs/journal/2026-03-05-team-mcp-enforcement-lessons-from-slock.md`).
- [ ] Verify Team conversation event-bus contract is runtime-effective: conversation chat input supports `conversation_id` scope without requiring user-supplied `run_id`/`from_actor_id`, `@member_id` routing is normalized server-side, execution-command message types still go through mailbox authority, and event timeline dedupe/replay uses stable `event_id` + `correlation_id`; record push + PR CI run IDs after merge (see `docs/features/team-conversation-event-bus.md`, `docs/journal/2026-03-05-team-conversation-event-bus-contract.md`).
- [ ] Verify Team terminology/process convergence against canonical operating model (`docs/features/agents-teams.md`): align user-facing wording (`Conversation` first), internal runtime terms (`Run`/`Step`/`Actor`), and status buckets (`working`/`idle`/`stopped`/`missing`) across Team docs/UI/API references; record push + PR CI run IDs after merge.
- [ ] Verify actor runtime alias behavior in local/manual flow: `agenthub actor` and `agenthub actor-mcp` accept `agent_id` aliases (`--agent-id`, `--from-agent-id`, `--to-agent-id`, `AGENTHUB_ACTOR_AGENT_ID`) while preserving required `run_id` partitioning; verify mailbox payload includes explicit `from_actor_kind` / `to_actor_kind` (`human|agent`) markers (see `docs/features/agents-teams.md`).
- [ ] Verify web Tailwind design-token rollout wave-1 remains stable across Team/ACP/Agents shared components (`tailwind.config.cjs` semantic tokens + `tailwind_classes.ts` preset migration) and record push + PR CI run IDs after merge (see `docs/features/frontend-design.md`).
- [ ] Verify `/teams` primary `Conversation` workflow is user-facing and does not expose internal `task` wording (create/select conversation, send planning messages with route modes, refresh message history, and one-click sync selected conversation into compile-run-preview); record push + PR CI run IDs after merge (see `docs/features/agents-teams.md`).
- [ ] Continue `docs/features` compact wave-2: merge remaining Team UI extraction/reducer phase notes into domain-level canonical specs and replace merged notes with `Superseded by ...` pointers (follow `docs/features/README.md`).
- [ ] Verify Team role responsibility contract is enforced in skills/prompts: leader behaves as architect/reviewer (no default feature-code implementation path), worker remains implementation executor, and leader-only emergency coding exceptions are explicitly documented when used (see `docs/features/agents-teams.md`).
- [ ] Verify Team leader planning quality gate is enforced in skills/prompts: `Decision Complete`, `Explore Before Asking`, and delegation clearance checklist are present and applied before worker dispatch (see `docs/features/agents-teams.md`).
- [ ] Verify Team coordination/finalization contract is enforced in skills/prompts: teammate routing uses `spec.members[].member_id`, TODO status tracking is kept current (including stale-entry compaction), and finalization behavior is mode-aware (`persistent` keep-running vs `one-shot` graceful shutdown) (see `docs/features/agents-teams.md`).
- [ ] Verify Team cold-start workflow guidance: leader/worker role skills and default team prompts require TODO-first startup checks (`TODO.md`, `.cache/context/todo.md`), align on six workflow phases (`team formation` -> `task analysis` -> `role assignment` -> `communication and collaboration` -> `consensus formation` -> `result integration`), enforce leader direct human-facing planning replies (no worker redirection), keep phase-aware coordination sections in `AGENTS.md`, maintain `AGENTS.md` as index/routing artifact (not procedural duplication of `SKILL.md` details), and apply per-agent TODO lifecycle rules (`pending`/`in_progress`/`completed`/`blocked`, one `in_progress` per agent, `completed` only with acceptance evidence); while `/teams` UI exposes the same operating model and startup playbook; record push + PR CI run IDs (see `docs/features/agents-teams.md`).
- [ ] Verify `/teams` selected-team top bar shows per-member lifecycle states (`working` / `idle` / `stopped` / `missing`) using Team spec membership + agent runtime status mapping, and remains visible even when no active run is selected; record push + PR CI run IDs after merge (see `docs/features/agents-teams.md`).
- [ ] Verify stale `running` agent status auto-reconciliation across API/SSE/send-input: when runtime handle is missing after abrupt restart/kill or direct `Ctrl+C`, `list/get agent`, SSE subscription, and send-input fallback should converge DB agent/session rows to `exited`, avoiding UI `running` + backend `agent not running` mismatch and stopping endless SSE retry loops; record push + PR CI run IDs (see `docs/journal/2026-02-23-agent-running-status-runtime-reconciliation.md`, `docs/journal/2026-03-08-sse-stale-running-agent-reconciliation.md`).
- [x] Verify backend DB/Agent error logging hardening adds structured logs for agent lifecycle fallback DB failures (startup/stop/send-input/exit finalization) and database init/cleanup failures, while preserving current behavior and passing targeted Rust tests (see `docs/journal/2026-02-23-backend-db-agent-error-logging-hardening.md`).
- [x] Verify backend non-router error visibility hardening adds API error-chain logging (`status` + full `anyhow` cause chain) and AgentManager runtime instrumentation (`#[tracing::instrument(err)]`) for start/worktree/send-input/stop/delete/code-mode paths so DB/agent failures are visible from module targets before router boundary logs (see `docs/journal/2026-02-23-backend-error-chain-and-agent-instrumentation.md`).
- [ ] Verify Team create flow persists browser-side draft with explicit `creating` status before final submit, restores same-mode draft (`Guided Wizard` or `Manual Spec`) on reopen, and clears draft after `Create Team` succeeds; ensure create-time failures (runtime defaults load, local draft parse/save) always surface visible error messages; validate wording alignment that worker is a role of forged agents (not a separate entity); verify successful create also cleans up unselected forged agents from the same draft session and surfaces warning details if cleanup fails; then record push + PR CI run IDs after merge (see `docs/features/agents-teams.md`).
- [ ] Verify Team Leader Forge can omit `workdir`: UI should auto-generate a path under `~/.agenthub/worktrees`, backend should create the missing directory at runtime start, and the flow should pass both `push` and `pull_request` CI before marking done (see `docs/journal/2026-02-23-team-leader-default-workdir-under-agenthub.md`).
- [ ] Verify auth/join error banner normalization: when backend returns JSON payload like `{\"error\":\"user not found\"}`, UI should display plain message (`user not found`) instead of raw JSON or `Error:`-prefixed wrapper; record push + PR CI run IDs after merge (see `docs/journal/2026-02-23-auth-error-message-normalization.md`).
- [ ] Verify Team Forge leader Agent modal hides worktree advanced controls and create payload always uses `worktree_mode=use_existing` (`worktree_repo=null`, `worktree_ref=null`) even if stale UI state exists; include Chrome DevTools MCP before/after snapshots once MCP transport recovers, and record push + PR CI run IDs after merge (see `docs/journal/2026-02-23-team-leader-forge-worktree-mode-lock.md`).
- [ ] Verify Teams sidebar filter UX (`Filter teams by name or id` + clear action + selected item `aria-current`) and mailbox helper coverage additions remain stable in Web CI (`lint`, unit tests, build) for both `push` and `pull_request` events (see `docs/journal/2026-02-23-team-sidebar-filter-and-mailbox-helper-coverage.md`).
- [ ] Verify `Create Agent` modal keeps common fields focused while `Advanced Options` stays collapsed by default (`use_existing`) and auto-expands for non-default worktree mode / worktree error paths; include Chrome DevTools MCP before/after snapshots once MCP transport recovers, and record push + PR CI run IDs after merge (see `docs/journal/2026-02-23-create-agent-modal-advanced-options.md`).
- [ ] Verify `DELETE /api/teams/:id` no longer returns `500` when member agents have dependent `agent_events` / `acp_permission_requests` rows, and confirm member cleanup + Team cascade run in one DB transaction boundary (no partial cleanup on mid-chain failure); record push + PR CI run IDs after merge (see `docs/features/backend-runtime-logic.md`).
- [ ] Verify Team run ownership hardening covers run-scoped APIs (`get/cancel/resume/restart/snapshot/events/steps/messages/context flush`) and that new shared crate `agenthub-text` remains stable in both Cargo and Bazel CI (see `docs/features/backend-runtime-logic.md`).
- [ ] [LOW] Defer Team ACL hardening (explicit owner/member permission matrix across Team task/run/mailbox APIs and UI visibility) until the chat-first baseline workflow and core Team features are complete.
- [ ] Verify single-mode ACP sessions no longer inject Team role skills from global `~/.agenthub/skills.json`, while Team actor sessions still inject `team-leader-orchestrator`/`team-worker-executor` + `team-deliberation-rules` by role context (see `docs/features/agents-teams.md`).
- [ ] Verify `scripts/setup_ck_search_context.sh` bootstraps `~/.agenthub/mcp.json` with `ck --serve` MCP server entry (`ck-search`) and preserves existing `mcpServers` entries without destructive overwrite (see `docs/journal/2026-02-22-ck-search-mcp-and-info-research-layout.md`).
- [ ] Verify local research layout bootstrap creates `.info/papers` and `.info/clippings` under configured `--info-root`, and keep the layout git-ignored while promoting adopted outcomes into tracked docs (see `docs/journal/2026-02-22-ck-search-mcp-and-info-research-layout.md`).
- [ ] Design review: lock Team long-horizon memory tiering (`L0` prompt working set / `L1` run episodic files / `L2` durable curated files), including schema ownership, `L1 -> L2` promotion rules, retrieval-budget allocation by prompt mode (`full`/`minimal`/`none`), and acceptance tests before Track 2 implementation starts (see `docs/journal/2026-02-22-team-context-memory-architecture.md`, `docs/journal/2026-02-22-team-memory-flush-spec.md`).
- [ ] Design review: publish Team context contract v1 (stable prefix vs dynamic tail, append-only context logs, and allowed-action gate policy) and align leader/worker prompt assembly against it (see `docs/journal/2026-02-22-team-context-memory-architecture.md`).
- [x] Implement filesystem-backed context memory layout baseline for Team runs (`.cache/context/run/<run_id>/...`) with pointer-first continuity offload and regression coverage in `team::manager::tests::complete_step_offloads_large_output_to_workspace_context_artifact` (see `docs/journal/2026-02-22-team-context-artifact-pointer-baseline.md`).
- [x] Implement Team run manual context flush baseline: backend API `POST /api/teams/runs/:run_id/context/flush`, artifact/checkpoint persistence, and lifecycle events (`memory_flush_started` / `memory_flush_persisted` / `memory_flush_noop` / `memory_flush_failed`) with manager/API regression coverage (see `docs/journal/2026-02-22-team-memory-flush-manual-baseline.md`).
- [ ] Implement pre-compaction automatic memory flush trigger integration for Team sessions (soft-threshold/hard-error paths) and compaction retry-path verification (source of truth: `docs/journal/2026-02-22-team-memory-flush-spec.md`).
- [ ] Implement workspace-scoped context isolation: each agent writes only to `<agent_workspace>/.cache/context`, cross-agent access goes through Team events/APIs, and leader runtime provisions/uses an empty dedicated workspace for context-management artifacts (see `docs/journal/2026-02-22-team-context-memory-architecture.md`).
- [ ] Verify Dependabot bootstrap (`.github/dependabot.yml`) creates weekly update PRs for `github-actions`, Cargo workspace, `web` npm dependencies, and `userdocs` npm dependencies after merge; record first successful run links in PR/issue evidence (see `docs/journal/2026-02-22-dependabot-bootstrap.md`).
- [ ] Verify Team context/memory continuity Track 1 baseline: per-member continuity envelope state is persisted and attached to subsequent Team runs by default (`inherit_recent`) without reusing prior process instances; include router/service/orchestrator coverage and one Team repeated-run regression case (see `docs/journal/2026-02-22-team-context-memory-architecture.md`).
- [ ] Verify Team context/memory continuity Track 3 controls and observability: run-level continuity mode (`inherit_recent`/`reset`) and bounded envelope knobs are respected, and continuity lifecycle events (`continuity_attached`/`continuity_reset`/`continuity_fallback`) are visible in Team Events/Debug with test coverage (see `docs/journal/2026-02-22-team-context-memory-architecture.md`).
- [ ] Design review: finalize Team context/memory continuity Track 2 (long-horizon memory schema, retention/redaction, retrieval/compaction policy) before implementation and publish a follow-up feature note with rollout/rollback plan (see `docs/journal/2026-02-22-team-context-memory-architecture.md`, `docs/journal/2026-02-22-team-memory-flush-spec.md`).
- [x] Verify PR #83 review hardening: dynamic Team/Agent API path segments are centralized with `encodeURIComponent` in `web/src/api.ts`, and `useTeamActions` lifecycle-facing callbacks (`refreshSteps`, `loadInbox`, `onLoadMoreRuns`) keep stable identity under non-token UI input churn with focused hook tests + web lint/build green (see `docs/journal/2026-02-22-pr83-review-hardening-path-encoding-and-callback-stability.md`).
- [x] Verify Team mailbox action callbacks (`onSendChatMessage` / `onSendMessage` / `onRefreshInbox` / `onAckMessage`) are extracted from `web/src/pages/team_page.tsx` into `useTeamMailboxActions`, with branch behavior tests and `web` lint/test/build green (see `docs/journal/2026-02-22-team-page-mailbox-actions-hook-extraction.md`).
- [x] Verify Team step operation callbacks (`onSubmitStep` / `onApplyStepAction`) are extracted from `web/src/pages/team_page.tsx` into `useTeamStepActions`, with focused payload/validation tests and `web` lint/test/build green (see `docs/journal/2026-02-22-team-page-step-actions-hook-extraction.md`).
- [x] Verify Team run/mailbox action callback extraction from `web/src/pages/team_page.tsx` into `useTeamActions` keeps behavior stable while reducing main-page callback density; add callback identity stability tests and keep `web` lint/test/build green (see `docs/journal/2026-02-21-team-page-actions-hook-and-stability-tests.md`).
- [x] Verify Team workbench adopts a conversation-first primary flow by removing `Create Run` controls from the primary `TeamRunPanel`, keeping run creation/loading in `Debug -> Run Ops`, and preserving run-browser interactions/tests (`team_panels`, `team_page.runs`) with web build green (see `docs/journal/2026-02-21-team-chat-first-run-ops-entry.md`).
- [x] Verify Team domain persists full task dialogue artifacts via explicit task/conversation models (for replay and handoff) while keeping runtime tokens/secrets out of SQLite records (see `docs/journal/2026-02-22-team-task-conversation-persistence.md`).
- [x] Verify leader orchestration compiles user-approved task discussion into deterministic Team run payload previews (fixed `spec.steps` template + role-bound assignments + acceptance criteria + optional deadline) before execution start (see `docs/journal/2026-02-22-team-task-compile-run-preview.md`).
- [x] Verify Team Debug Run Ops can compile task payload previews and apply preview payload into `Create Run` (including direct `Create Run from Preview`), with web lint/build and Team unit tests green (see `docs/journal/2026-02-22-team-debug-run-ops-compile-preview-ui.md`).
- [x] Verify Team chat supports three routing modes with ordered mailbox replay: `to_leader`, `to_member`, and `group_chat`, and model the human user as a first-class special actor for consistent message semantics (see `docs/journal/2026-02-22-team-task-routing-user-actor-semantics.md`).
- [x] Verify new Playwright case for Team compile-preview Run Ops (`team debug run ops compiles task preview and applies payload to create-run form`) and record CI run ID (`Web E2E` run `22275156889`, job `64435982892`) (see `docs/journal/2026-02-22-team-debug-run-ops-compile-preview-ui.md`, `docs/journal/2026-02-22-team-chatfirst-create-flow-e2e-hardening.md`).
- [x] Verify A2A-style agent discovery card exposure (`/.well-known/...`) and Team-side capability rendering so members can discover peer identity/capability metadata before inter-agent coordination (see `docs/journal/2026-02-22-team-a2a-discovery-card-capability-rendering.md`).
- [x] Verify end-to-end chat-first delivery path in Playwright (`task -> leader negotiation -> run compile -> worker execution -> final synthesis`) while preserving existing Debug run-ops regression behavior; CI run-ID evidence recorded (`Web E2E` run `22275156889`, job `64435982892`) (see `docs/journal/2026-02-22-team-chat-first-golden-path-e2e-case.md`, `docs/journal/2026-02-22-team-chatfirst-create-flow-e2e-hardening.md`).
- [x] Verify Team spec member-agent backfill side effect is extracted from `TeamPage` into dedicated hook (`useTeamMemberAgentBackfillEffect`) and add focused coverage for `useTeamCreateModalLifecycleEffects` branches (runtime default root fallback/fetch normalization, leader fallback, Escape close busy-guard), while keeping web test/lint/build green (see `docs/journal/2026-02-21-team-page-member-backfill-hook-and-create-hook-tests.md`).
- [x] Verify Team create-modal/runtime-default lifecycle side effects are extracted from `TeamPage` into dedicated hook (`useTeamCreateModalLifecycleEffects`) while preserving runtime default worktree-root bootstrap, modal leader fallback selection, and `Escape` close guard behavior; keep web test/lint/build green (see `docs/journal/2026-02-21-team-page-create-modal-lifecycle-effects-hook.md`).
- [x] Verify Team mailbox/member lifecycle side effects are extracted from `TeamPage` into dedicated hook (`useTeamMailboxLifecycleEffects`) while preserving snapshot-member fallback, inbox auto-load, mailbox auto-scroll/seen sync, and member-events refresh behavior; keep web test/lint/build green (see `docs/journal/2026-02-21-team-page-mailbox-lifecycle-effects-hook.md`).
- [x] Verify Team run lifecycle side effects are extracted from `TeamPage` into dedicated hook (`useTeamRunLifecycleEffects`) while preserving bootstrap/loading/polling behavior, and add focused run-helper coverage (`resolveActiveRunIdForSelectedTeam`) with web lint/build green (see `docs/journal/2026-02-21-team-page-run-lifecycle-effects-hook.md`).
- [x] Verify Team default role prompts are extracted into dedicated crate `agenthub-team-prompts` (remove large inline prompt constants from `src/api/teams.rs`), and Team spec default prompt injection still resolves by role (`leader`/`worker`) with Rust checks green (see `docs/journal/2026-02-21-team-default-prompts-crate-extraction.md`).
- [x] Verify Teams usability polish keeps run/member interactions stable while improving discoverability: member strip now shows member IDs + lifecycle status chips (not only role dots), run input editor provides explicit context/input hints with JSON quick actions (`Use Example JSON` / `Set Empty Object` / `Format JSON` / `Clear`), inline parse error feedback, and `Ctrl/Cmd + Enter` submit shortcut, right-pane content (`Active Run` + tabs) remains scrollable via `teams-main` overflow tuning, and redundant Team grid/panel overrides in `web/src/styles.css` no longer fight component Tailwind classes; validation includes Team panel/run tests + web lint/build + Chrome DevTools MCP snapshot checks (see `docs/journal/2026-02-21-team-usability-polish-and-scroll-hardening.md`).
- [ ] Verify Bazel target for new Team domain crate (`//crates/agenthub-team-domain:agenthub_team_domain`) in CI/clean environment after phase-3 crate migration, because local Bazel cache setup currently fails on `rules_rust` repository resolution (see `docs/journal/2026-02-21-team-domain-crate-migration-phase3.md`).
- [x] Verify phase-3 Team domain crate migration keeps Team API/runtime behavior unchanged while moving domain models to `crates/agenthub-team-domain` and preserving re-export compatibility in `src/team/mod.rs` (see `docs/journal/2026-02-21-team-domain-crate-migration-phase3.md`).
- [x] Verify `src/api/teams.rs` error mapping module split (`src/api/teams/errors.rs`) keeps duplicate-name conflict, resume/restart conflict mapping, and router HTTP contract behavior unchanged (see `docs/features/backend-runtime-logic.md`).
- [x] Verify typed-error mapping replaces string-based conflict detection for Team run resume (`completed` => `409`) and Agent send-input stale session (`session mismatch` => `409`) with focused Rust tests (see `docs/journal/2026-02-21-rust-error-typing-teams-agents.md`).
- [ ] Verify Bazel-first Rust crate decomposition policy is applied to new domain migrations: extract cohesive domain libraries under `crates/*`, keep `src/main.rs` thin-bootstrap only, and align Bazel package/target boundaries with crate boundaries in the same PR (see `docs/journal/2026-02-21-bazel-first-domain-crate-policy.md`).
- [x] Verify Teams workbench style isolation after Tailwind v4 migration: legacy global form-control selectors (`button/input/textarea/select:not([class*="mantine-"])`) no longer override `/teams` Tailwind controls, and Teams shell layout remains stable after removing legacy `app` wrapper coupling (`team_page.tsx`, `styles.css`) with web Team regression tests + web build green (see `docs/journal/2026-02-20-team-page-legacy-form-style-isolation.md`).
- [x] Verify Tailwind v4 border-color compatibility guard restores neutral default border color (avoid unexpected dark frames from `currentColor`) via `web/src/tailwind.css` base-layer patch, while keeping `web` lint/build and Team regression tests green (see `docs/journal/2026-02-20-web-tailwind-v4-border-color-compat.md`).
- [x] Verify Team shell/style migration further retires redundant Team layout CSS from `web/src/styles.css` (`teams-layout/sidebar/main/form/list/member-dot` blocks) by moving these concerns to Tailwind classes in Team components while keeping `web` lint/build and Team panel regression tests green (see `docs/journal/2026-02-20-team-style-css-further-slimming.md`).
- [x] Verify web Tailwind baseline upgrades from `tailwindcss@^3.4.17` to `tailwindcss@^4.2.0` with PostCSS plugin migration to `@tailwindcss/postcss`, while keeping `web` lint/build and Team panel regression tests green (see `docs/journal/2026-02-20-web-tailwind-v4-upgrade.md`).
- [x] Verify Team run controls reduce primary-surface density by keeping `Create Run` in the main panel and moving `Load Run (run_id)` into `Debug -> Run Ops`, while Team panel regression tests stay green (see `docs/journal/2026-02-20-team-load-run-moved-to-debug-run-ops.md`).
- [x] Verify Team/ACP UI no longer depends on legacy global style hierarchy selectors (`.card`, `.tab`, `.tab-bar`, `.team-tab-bar`, `.toolbar`, `.toolbar-actions`, `.actions`, `.team-item.active`, nested `.acp-tabs .tab*`) by migrating panel/list/tab styling to Tailwind/component-scoped classes and keeping `web` lint + panel regression tests green (see `docs/journal/2026-02-20-team-acp-global-style-layer-retirement.md`).
- [x] Verify Team workbench no longer collapses/overlaps after post-load refreshes by removing Team page dependence on legacy global `toolbar/actions/tab-bar` layering and scoping legacy `.card ul` max-height rules to Admin only (see `docs/journal/2026-02-20-team-post-load-layout-collapse-guard.md`).
- [x] Verify Team workbench run scope is team-bound: selecting a team in the left sidebar scopes output rendering and `Load Run` / `Resume Run` / `Restart Run` to that team only (no implicit team switch on run lookup), while step/message/inbox actions operate on the selected team's active run context (see `docs/journal/2026-02-20-team-selected-team-run-scope.md`).
- [x] Verify Team workbench introduces a dedicated `Debug` tab with function tags (`Step Ops`, `Mailbox Raw`) so step mutation controls and raw mailbox JSON/inbox tools are partitioned from primary `Steps`/`Mailbox` flows, while panel modes and Team mailbox E2E remain stable (see `docs/journal/2026-02-20-team-debug-tab-split-advanced-tools.md`).
- [x] Verify Team run panel health simplification uses compact member dots (`green=active`, `red=inactive/missing`) with `team_number` summary and visible role badges (`L` leader / `W` worker, leader first), Member Console exposes `mcp_skills`, long desktop fields no longer cause panel overlap (run meta/chat head/member detail prompt/skills), and Team workbench refresh actions use compact icon+label buttons with accessible labels (see `docs/journal/2026-02-20-team-member-dot-health-and-mcp-skill-display.md`).
- [x] Verify Team role workflow policy is enforced across prompt defaults and runtime startup gates (leader: `use_existing` + empty workspace; worker: `create_worktree` + isolated run workdir + random branch) with Rust/Web regression tests (see `docs/features/agents-teams.md`).
- [x] Refactor ACP-related code into package-style module layout (`src/acp/mod.rs` + `event_sink.rs` + `runtime.rs`) and move service bootstrap out of `lib.rs` into `src/app.rs` while preserving `main` thin-entry behavior (`agenthub::run`) and ACP runtime test coverage (see `docs/journal/2026-02-20-acp-package-and-bootstrap-module-migration.md`).
- [x] Refactor OpenAPI route module into package layout (`src/api/openapi/mod.rs` + `docs_page.rs` + `tests.rs`) and remove monolithic `src/api/openapi.rs` while keeping `/api/openapi.json` and `/api/openapi/docs` behavior unchanged (see `docs/journal/2026-02-20-openapi-package-module-migration.md`).
- [x] Refactor OpenAPI spec assembly into dedicated module `src/api/openapi/spec.rs` to keep OpenAPI route module focused on routing/auth/docs-page handlers, and verify OpenAPI auth/path contract tests remain stable (see `docs/journal/2026-02-20-openapi-spec-module-migration.md`).
- [ ] Verify periodic history retention cleanup (`history.event_retention_days`) deletes `agent_events` older than configured window (default `5` days) using bounded batch delete (`history.delete_batch_size`) at configured interval (`history.cleanup_interval_seconds`), and optional `history.vacuum_on_cleanup` compaction behavior remains stable on non-empty cleanup runs (see `docs/journal/2026-02-20-sqlite-history-retention-cleanup.md`, `docs/journal/2026-02-26-history-cleanup-interval-batch-delete.md`).
- [x] Verify `DELETE /api/teams/:id` also cleans Team member agent sessions (`agent_sessions`) by member IDs from Team spec and preserves Team data cascade behavior (`team_runs/steps/events/messages`) (see `docs/journal/2026-02-20-team-delete-cleans-member-agent-sessions.md`).
- [x] Verify Team run workbench readability layout refinement keeps interaction semantics unchanged while reducing stacked density (member health block + run controls/list responsive split) and preserves web checks + mobile Team E2E (see `docs/journal/2026-02-20-team-run-panel-readability-layout.md`).
- [x] Verify Team mobile workbench remains usable with long member IDs / long JSON payload lines (no horizontal overflow, toolbar actions wrap, and conversation/event/member `pre` blocks remain readable) while keeping Team tests and web checks green (see `docs/journal/2026-02-20-team-mobile-overflow-hardening.md`).
- [x] Verify `TeamMemberConsolePanel` and `TeamMailboxPanel` reuse shared Team Tailwind panel constants while keeping panel interaction tests green (`team_panels`) and passing `web` lint/build (see `docs/journal/2026-02-20-web-team-member-mailbox-shared-tailwind-classes.md`).
- [x] Verify Team run-level `resume/restart` strategy in a real restart scenario: after AgentHub process restart, failed/canceled runs can be resumed into a fresh submitted run, completed runs reject resume with `409`, and restart always forks a new submitted run with preserved `context_id`/`input` (`teams_router_resume_restart_strategy_survives_state_reopen` in `src/api/teams/tests_router.rs`; see `docs/journal/2026-02-20-team-run-resume-restart-strategy.md`).
- [ ] Verify Codecov upload strict mode (`fail_ci_if_error: true`) stays stable for `Rust`/`Web`/`Web E2E` workflows on both `push` and `pull_request` events; record workflow run IDs before marking done (see `docs/journal/2026-02-20-ci-codecov-fail-fast-upload.md`).
- [x] Verify Team workbench shared Tailwind class extraction across `TeamRunPanel`/`TeamOverviewPanel`/`TeamEventsPanel`/`TeamStepsPanel` keeps interaction tests green (`team_panels`), passes `web` lint/build, and preserves existing callback/reducer behavior (see `docs/journal/2026-02-20-web-team-panel-shared-tailwind-classes.md`).
- [x] Verify auth/login/join pages consume shared Tailwind auth class constants and keep frontend checks green (`build` + `lint`) after maintainability refactor; keep low-risk maintainability review suggestions as direct in-PR fixes by default (see `docs/journal/2026-02-20-auth-tailwind-shared-class-maintainability.md`).
- [x] Verify Team mailbox E2E `Mailbox` tab remains clickable after Team run panel/UI style updates and fix E2E coverage merge URL-scheme errors (`ERR_INVALID_URL_SCHEME`) so `web e2e:coverage` emits numeric coverage summary (see `docs/journal/2026-02-20-web-e2e-mailbox-tab-and-coverage-merge-fix.md`).
- [x] Verify ACP tool-call summary alignment uses top-aligned layout (no vertical centering) across desktop/mobile for single call, grouped calls, and explore groups after restoring semantic style control (see `docs/journal/2026-02-20-acp-tool-call-summary-top-align.md`).
- [x] Verify Tailwind UI migration phase-10 visual parity for admin/runtime shell (`AdminPage`, `ErrorBanner`, `TerminalOutput`) across desktop/mobile while preserving admin tab workflows, error-dismiss behavior, and terminal output stream readability (see `docs/journal/2026-02-20-web-tailwind-ui-phase10-admin-error-terminal-shell.md`).
- [x] Verify Tailwind UI migration phase-9 visual parity for `AcpConversation` shell (conversation viewport, message bubble tones, tool-call fold summaries, plan card, and segmented payload presentation) across desktop/mobile while preserving fold interaction, payload filtering, and render-test compatibility (see `docs/journal/2026-02-20-web-tailwind-ui-phase9-acp-conversation-shell.md`, `docs/journal/2026-02-20-web-tailwind-ui-phase1-9-closeout-verification.md`).
- [x] Verify Tailwind UI migration phase-8 visual parity for ACP shell (`AcpPanel` / `AcpDebug`) including tab states, session controls, permissions copy/jump actions, runtime metric cards, and raw event list scrolling across desktop/mobile while preserving ACP debug behavior (see `docs/journal/2026-02-20-web-tailwind-ui-phase8-acp-panel-debug-shell.md`, `docs/journal/2026-02-20-web-tailwind-ui-phase1-9-closeout-verification.md`).
- [x] Verify Tailwind UI migration phase-7 visual parity for `TeamPage` shell and `Team Forge` modal (stage nav, tab switcher, form controls, worker cards, action bar) across desktop/mobile while preserving create-team wizard behavior and existing stage validation rules (see `docs/journal/2026-02-20-web-tailwind-ui-phase7-team-page-modal-shell.md`, `docs/journal/2026-02-20-web-tailwind-ui-phase1-9-closeout-verification.md`).
- [x] Verify Tailwind UI migration phase-6 visual parity for `OutputBody` / `OutputErrorBoundary` / `InputDock` (loading/empty/error states, history menu, interrupt/send affordance, and dock spacing) across desktop/mobile while preserving keyboard shortcuts and input history behavior (see `docs/journal/2026-02-20-web-tailwind-ui-phase6-output-and-input-dock.md`, `docs/journal/2026-02-20-web-tailwind-ui-phase1-9-closeout-verification.md`).
- [x] Verify Tailwind UI migration phase-5 visual parity for `AgentsPanel` / `OutputHeader` (expanded/collapsed states, agent-row active state, sidebar actions, output meta chips, and subtitle visibility) across desktop/mobile while preserving existing agents interactions and ACP subtitle behavior (see `docs/journal/2026-02-20-web-tailwind-ui-phase5-agents-output-header.md`, `docs/journal/2026-02-20-web-tailwind-ui-phase1-9-closeout-verification.md`).
- [x] Verify Tailwind UI migration phase-4 visual parity for `TeamMemberConsolePanel` / `TeamMailboxPanel` (member selector, conversation bubbles, compose/advanced controls, inbox actions, and unread indicators) across desktop/mobile while preserving mailbox/member interaction behavior (see `docs/journal/2026-02-20-web-tailwind-ui-phase4-team-member-mailbox.md`, `docs/journal/2026-02-20-web-tailwind-ui-phase1-9-closeout-verification.md`).
- [x] Verify Tailwind UI migration phase-3 visual parity for `TeamOverviewPanel` / `TeamEventsPanel` / `TeamStepsPanel` (toolbar controls, list surfaces, form controls, and action button states) across desktop/mobile while preserving existing step/event interactions (see `docs/journal/2026-02-20-web-tailwind-ui-phase3-team-overview-events-steps.md`, `docs/journal/2026-02-20-web-tailwind-ui-phase1-9-closeout-verification.md`).
- [x] Verify Tailwind UI migration phase-2 visual parity for `TeamRunPanel` (team header/delete action, create/load controls, run-list filter/refresh/load-more states) across desktop/mobile and preserve existing run interactions/filter semantics (see `docs/journal/2026-02-20-web-tailwind-ui-phase2-team-run-panel.md`, `docs/journal/2026-02-20-web-tailwind-ui-phase1-9-closeout-verification.md`).
- [x] Verify frontend contributions after 2026-02-20 comply with AGENTS policy (`@mantine/core` + Tailwind CSS default path, no new large handcrafted global CSS blocks) and capture enforcement evidence in PR reviews (see `docs/journal/2026-02-20-frontend-ui-library-tailwind-policy.md`, `docs/journal/2026-02-20-web-tailwind-ui-phase1-9-closeout-verification.md`).
- [x] Verify Tailwind UI migration phase-1 visual parity for auth/join cards and Team sidebar forge entry (`Guided Wizard` / `Manual Spec`) across desktop/mobile, including hover/focus/disabled states and existing Team sidebar interactions (see `docs/journal/2026-02-20-web-tailwind-ui-phase1-auth-team-sidebar.md`, `docs/journal/2026-02-20-web-tailwind-ui-phase1-9-closeout-verification.md`).
- [x] Verify Tailwind CSS baseline integration (`tailwindcss` + `postcss` + entry `tailwind.css`) works with existing Mantine/custom CSS without visual regressions on `Agents`, `Teams`, and login/join flows, and define phased migration scope for legacy style blocks (see `docs/journal/2026-02-20-web-tailwind-baseline-integration.md`, `docs/journal/2026-02-20-web-tailwind-ui-phase1-9-closeout-verification.md`).
- [ ] Verify Linux CI jobs (`Rust`, `Clippy`, `Bazel`) pass after `codex-linux-sandbox` CI hardening: add `libcap-dev` for `pkg-config` (`libcap.pc`) and ensure Bazel mid-term override (`crate.annotation` + `third_party/codex_linux_sandbox` vendored bwrap FFI) is stable; record push/PR run IDs (see `docs/journal/2026-02-19-ci-libcap-linux-sandbox.md`).
- [x] Pin `agenthub-codex-acp` codex git dependencies to fixed commit `c34b30a3c128bb75fcec27ef838c93c99b92fc61` (replace `branch = "acp"` with `rev`) to avoid upstream branch drift breaking ACP adapter builds (see `docs/journal/2026-02-19-codex-acp-codex-git-rev-pin.md`).
- [x] Verify `agenthub-codex-acp` compiles after codex upstream sync tied to `3b80a66ff118f19cdfeab21511caa61924982d29` (API path moves + protocol/config shape changes: `codex_utils_cli`, `codex_utils_approval_presets`, `Config.permissions`, `turn_id/status`, `McpServerConfig.required`, `RolloutRecorder::list_threads(&Config, ...)`) (see `docs/journal/2026-02-19-codex-acp-upstream-sync-3b80a66-api-fixes.md`).
- [ ] Verify codex-acp upstream PR160 sync in `agenthub-codex-acp` after lockfile upgrade (`c34b30a`): patch approval uses `call_id`, exec approval uses `approval_id` fallback + `turn_id`, `ModelReroute` events are handled, and model preset lookup still works across config/options/model APIs (see `docs/journal/2026-02-19-codex-acp-upstream-sync-pr160.md`).
- [x] Verify Team helper extraction coverage guardrails with dedicated unit tests for `create_helpers`/`state`/`page_helpers` (including parse error branches, reducer edge transitions, and upsert ordering semantics) and keep `web` test suite green (see `docs/journal/2026-02-19-team-helper-coverage-hardening.md`).
- [x] Verify Team page helper extraction keeps run/mailbox/member/state/create/page helper contracts stable after splitting into `web/src/pages/team/*` modules, while preserving existing `team_page.runs.test.ts` imports via `team_page.tsx` re-export (see `docs/journal/2026-02-19-team-page-helper-modularization.md`).
- [x] Verify Team create wizard removes manual `bind_target` for Agent Forge, derives `role_tag` from current stage (`Leader Forge` => leader, `Recruit Workers` => worker), and blocks forge action in Mission/Launch stages (see `docs/journal/2026-02-19-team-forge-role-tag-no-bind.md`).
- [x] Verify Team Forge-created agents are tagged with `source=team_forge` and hidden from `/api/agents` list while manual-created agents remain visible (see `docs/journal/2026-02-19-agent-source-separation-team-forge.md`).
- [x] Verify Teams page no longer marks member agents as `missing` when they are intentionally filtered from `/api/agents` (for example `team_forge` or active team-member runtime filters) by backfilling with `/api/agents/:id` lookup (see `docs/journal/2026-02-20-team-member-missing-status-backfill.md`).
- [ ] Verify Team context/memory continuity umbrella milestone closes after Track 1 + Track 3 implementation evidence and Track 2 design sign-off are all complete (see `docs/journal/2026-02-22-team-context-memory-architecture.md`).
- [x] Verify Team quant full-flow E2E (create team with leader resource-control + portfolio worker + crypto-algo worker, then launch run) and keep existing Team page regressions green (see `docs/journal/2026-02-19-team-quant-full-flow-e2e.md`).
- [x] Verify Agents page terminal mode restores `Jump to bottom` visibility/behavior after scrolling up (non-ACP sessions) while preserving ACP jump behavior (see `docs/journal/2026-02-19-agents-terminal-jump-bottom-restore.md`).
- [x] Verify Team mailbox exposes explicit `Jump to bottom` action and the button re-enables auto-follow + seen marker sync after manual scroll-up (see `docs/journal/2026-02-19-team-mailbox-jump-bottom-button.md`).
- [x] Verify Team Forge `Create Agent` modal matches Agents modal layering (`withinPortal`) and remains clickable/usable on desktop + mobile Team wizard flows; add explicit Playwright portal-layer assertion in Team Forge flow (see `docs/journal/2026-02-19-team-forge-create-agent-modal-portal-parity.md`, `docs/journal/2026-02-22-team-chatfirst-create-flow-e2e-hardening.md`).
- [x] Verify `Create Team` exposes dual entry points (`Guided Wizard` + `Manual Spec`) and removes in-wizard manual-mode toggles while preserving default guided validations and manual entry direct jump to Launch Team (manual path now hides in-modal forge controls and keeps direct Mission Brief -> Launch flow) (see `docs/journal/2026-02-19-team-create-dual-entry-modes.md`, `docs/journal/2026-02-22-team-chatfirst-create-flow-e2e-hardening.md`).
- [x] Verify Team page mobile proportions remain usable by collapsing Team workbench to single-column layout on narrow viewports (sidebar/main/chat/member rows/filter controls) with Playwright coverage (`team_page.e2e.ts`) (see `docs/journal/2026-02-19-team-mobile-layout-proportion-fix.md`).
- [x] Verify extracted Team panel components keep callback wiring and conditional rendering stable with dedicated jsdom interaction tests (`TeamSidebar`, `TeamRunPanel`, `TeamStepsPanel`, `TeamEventsPanel`, `TeamOverviewPanel`, `TeamMemberConsolePanel`, `TeamMailboxPanel`) and improve web coverage after component split (see `docs/journal/2026-02-19-team-panel-coverage-boost.md`).
- [x] Verify Team actor sessions auto-inject role skills in ACP prompt without requiring prior `scripts/setup_team_skills.sh` execution (leader => `team-leader-orchestrator`, worker => `team-worker-executor`, both include `team-deliberation-rules`) and ensure duplicate skill blocks are not emitted when `skills.json` already contains same entries (see `docs/features/agents-teams.md`).
- [x] Verify Team create skill selector enforces role-required skills (`agenthub-actor-runtime` + `team-leader-orchestrator`/`team-worker-executor`) by preventing deselection in UI and by backend auto-injection when `spec.members[].skills` omits required role skills (see `docs/journal/2026-02-19-team-required-role-skills-enforcement.md`).
- [x] Verify new Team skill `team-deliberation-rules` is included in Team default leader/worker skills and appended by `scripts/setup_team_skills.sh` into `~/.agenthub/skills.json` without dropping existing skill entries (see `docs/features/agents-teams.md`).
- [ ] Verify `scripts/setup_team_skills.sh` single-node default install mode (`~/.agenthub/worktrees/team-skills`) keeps Team role skills loadable in fresh local environments without extra `safe_paths` edits (see `docs/features/agents-teams.md`).
- [x] Verify Team create flow restricts leader/worker selection to agents forged in the current Team create session (no selection from pre-existing Agent inventory), and that forge candidate pool resets on modal reopen (see `docs/journal/2026-02-18-team-forge-only-member-selection.md`).
- [x] Verify Team spec enforces explicit member roles (`spec.members[].role` required, only `leader|worker`) across create-team API, router contract tests, and Team run lifecycle flows (see `docs/journal/2026-02-18-team-member-role-required.md`).
- [x] Verify `/api/agents` hides member agents that are currently executing Team steps (`team_steps.status in working/input_required`) while keeping non-Team agents visible (see `docs/journal/2026-02-18-team-agent-list-runtime-isolation.md`).
- [x] Verify Agent/Team shared status design language: `StatusBadge` tone mapping (`active/inactive/warning/danger/neutral`) renders consistently across Agents list, output header, and Teams run/member status chips, including active-row contrast in `/teams` (see `docs/journal/2026-02-18-agent-team-shared-status-design-language.md`).
- [x] Verify Team mailbox IM mode: member-card click opens mailbox chat, conversation auto-follow sticks to bottom unless user scrolls up, per-member unread counters update correctly, and mailbox auto-refresh uses conversation-focused polling while advanced JSON controls remain functional (see `docs/journal/2026-02-18-team-mailbox-im-chat.md`).
- [x] Verify send-input reliability under active SSE connections: after posting input, frontend boost polling can recover missed events, and backend closes lagged SSE streams to force reconnect + replay (see `docs/journal/2026-02-18-send-input-sse-lagged-recovery.md`).
- [x] Verify web UI shows explicit unexpected-exit alert when active agent status transitions from `running/idle` to `failed/exited`, while normal `stopped` remains silent (see `docs/journal/2026-02-18-agent-unexpected-exit-alert.md`).
- [x] Verify ACP-native actor mailbox tools (`actor_send`/`actor_inbox`/`actor_ack`) can run agent collaboration flows in local mode without requiring `AGENTHUB_ACTOR_CLI` (see `docs/journal/2026-02-18-acp-actor-mailbox-native-tools.md`).
- [x] Verify ACP actor runtime auto-injects `agenthub-actor-mailbox` MCP server (`actor-mcp`) and tool calls succeed end-to-end (`tools/list` + `tools/call`) in a real Codex ACP session (automated coverage via `load_mcp_servers_*` + `jsonrpc_tools_list_and_call_drive_local_mailbox_flow`; see `docs/journal/2026-02-18-acp-actor-mailbox-native-tools.md`).
- [x] Verify distributed actor transport (A2A) keeps mailbox semantics consistent with local mode for idempotency, bounded retries (no infinite retry), and dead-letter transitions (see `docs/features/actor-foundation.md`).
- [x] Verify actor mailbox API implementation matches documented contract for `actor_send`/`actor_inbox`/`actor_ack` request fields, state transitions, and HTTP error mapping (`400/401/403/404/409/410/422/429`) (see `docs/features/actor-foundation.md`).
- [x] Verify Teams mailbox REST endpoints remain wire-compatible after switching to `ActorMailboxService` adapter (`send`/`inbox`/`ack` response payloads and idempotency behavior) (see `docs/features/actor-foundation.md`).
- [x] Verify Team internal gRPC mailbox endpoints (`send_actor_message` / `list_actor_inbox` / `ack_actor_message`) remain wire-compatible after switching to `ActorMailboxService` adapter (status mapping + payload fields) (see `docs/features/actor-foundation.md`).
- [ ] Verify stale-session `send input` auto-recovery in real browser sessions: when frontend posts an outdated `session_id`, backend returns `409 conflict`, UI switches to running session, retries once, and message flow resumes without manual refresh (see `docs/journal/2026-02-18-send-input-session-guard-and-acp-type-compat.md`).
- [x] Verify latest codex ACP event types (`plan`, `available_commands`, `current_mode`, `run_status`) are persisted/rendered as ACP stream (not stderr/system) for ACP agents while non-ACP agents keep stderr classification unchanged (see `docs/journal/2026-02-18-send-input-session-guard-and-acp-type-compat.md`).
- [x] Verify mobile/tablet expanded agents drawer exposes an in-panel `Hide agents` control and can be collapsed without tapping backdrop (see `docs/journal/2026-02-17-mobile-agents-collapse-access.md`).
- [x] Verify focused message textarea remains visible during mobile virtual-keyboard viewport transitions (resize/pan) and does not drop below visible area while typing (see `docs/journal/2026-02-17-mobile-input-focus-visibility.md`).
- [x] Verify conversation auto-stick keeps following bottom during live content growth when user has not intentionally scrolled up, and only detaches after meaningful upward movement (see `docs/journal/2026-02-17-conversation-stickiness-hysteresis.md`).
- [x] Verify Playwright E2E coverage upload publishes `web-e2e` flag with `web/coverage/e2e/lcov.info` in CI push/PR runs (see `docs/journal/2026-02-17-playwright-e2e-coverage-codecov.md`).
- [x] Verify ACP conversation groups consecutive tool calls under one shared fold, preserves nested tool-call jump targeting, and keeps fold animation lightweight (`conversation.test.ts`, `acp_conversation_render.test.tsx`, `use_acp_conversation*.test.ts`) (see `docs/journal/2026-02-17-acp-tool-call-group-fold-animation.md`).
- [ ] Verify app shell uses runtime viewport sizing (`--agenthub-vh/--agenthub-vw`) and workspace-top anchoring on iOS/Android tablet+phone (address bar expand/collapse, keyboard open/close, orientation changes) without layout clipping or drawer offset drift (see `docs/journal/2026-02-16-web-mobile-viewport-alignment.md`).
- [x] Verify collapsed agents rail and expanded agent rows both surface pending ACP permissions with red indicators during active permission polling and agent switching (see `docs/journal/2026-02-17-agents-interaction-polish.md`).
- [x] Verify agent switch output hydration no longer waits for a second fetch when resolving `latest` session ids, and terminal auto-follow stays at bottom during in-place output updates (see `docs/journal/2026-02-17-agents-interaction-polish.md`).
- [x] Verify mobile text input no longer triggers white-screen shell collapse when keyboard transitions cause transient `visualViewport` anomalies (`0/1` or non-finite dimensions) (see `docs/journal/2026-02-17-mobile-input-white-screen-guard.md`).
- [x] Verify mobile/tablet workspace keeps input dock pinned to bottom of output panel under agent list collapsed/expanded states and keyboard open/close transitions (see `docs/journal/2026-02-17-mobile-input-dock-bottom-alignment.md`).
- [ ] Verify ACP filesystem guard resolves relative `apply_patch` paths against session root (not process CWD) and rejects traversal outside root, matching upstream codex-acp behavior (see `docs/journal/2026-02-17-codex-acp-upstream-sync-path-ordering.md`).
- [ ] Verify ACP tool-call/debug JSON preserves key ordering for keychain/MCP-sensitive payloads by keeping `serde_json/preserve_order` enabled in `agenthub-codex-acp` (see `docs/journal/2026-02-17-codex-acp-upstream-sync-path-ordering.md`).
- [ ] Verify ACP prompt state supports concurrent `exec_command` streams without losing active command state when begin/end events interleave by `call_id` (see `docs/journal/2026-02-17-codex-acp-upstream-sync-thread-state.md`).
- [ ] Verify `/compact` and `/undo` run through unified `PromptState` handling after `TaskState` removal, including turn completion and undo status messaging (see `docs/journal/2026-02-17-codex-acp-upstream-sync-thread-state.md`).
- [ ] Verify web Codecov patch gate remains above threshold after additional `app.tsx`/ACP interaction changes; keep targeted branch tests in `app.permission_scope`, `acp_debug.interaction`, and `use_acp_conversation.interaction` up to date (see `docs/journal/2026-02-16-web-coverage-review-followups.md`).
- [ ] Verify newly added jsdom interaction tests for ACP debug copy/jump and app viewport sync stay stable in CI runners (no localStorage/env-specific flakes) (see `docs/journal/2026-02-16-web-coverage-review-followups.md`).
- [ ] Verify ACP Debug permission history row click jumps to the linked Conversation tool call bubble with preserved surrounding context and temporary highlight across long virtualized sessions (see `docs/journal/2026-02-16-acp-permission-history-jump-context.md`).
- [ ] Verify ACP Debug permission history copy-to-clipboard action works across desktop/mobile browsers with clipboard API fallback behavior (see `docs/journal/2026-02-16-acp-permission-history-bubble-copy.md`).
- [x] Verify ACP permission pending/history views remain scoped to the active agent during agent switching and in-flight permission polling (see `docs/journal/2026-02-16-permission-history-agent-scope.md`).
- [x] Verify dedicated Clippy workflow (`.github/workflows/clippy.yml`) runs on push/PR and enforces `cargo clippy --workspace --all-targets -- -D warnings` as an independent CI gate (see `docs/journal/2026-02-16-cargo-clippy-zero-warning-and-ci-gate.md`).
- [ ] Verify Rust workflow cache hit behavior after adding `Swatinem/rust-cache` and cache-aware `cargo-llvm-cov` setup (3-5 runs) and confirm no regression in `rust-cargo` coverage upload (see `docs/journal/2026-02-17-rust-ci-cache-expansion.md`).
- [ ] Verify refreshed README/docs navigation and command snippets (`README.md`, `docs/README.md`, `userdocs/README.md`) remain aligned with current scripts and deployment workflow (see `docs/journal/2026-02-16-readme-docs-structure-refresh.md`).
- [ ] Verify new userdocs `Deployment` and `Advanced Usage` tracks (sidebar order, cross-links, and page accuracy) in real browser navigation (see `docs/journal/2026-02-16-userdocs-deployment-advanced-expansion.md`).
- [ ] Verify local toolchain bootstrap from `rust-toolchain.toml` installs required components (`rustc`, `cargo`, `std`, `rustfmt`, `clippy`) on clean machines without manual `rustup component add` (see `docs/journal/2026-02-16-rust-toolchain-components-local-compile.md`).
- [ ] Verify storage quota guard in real browser: when `localStorage` is near/full, output cache persistence degrades gracefully and app actions (send/login/join) no longer surface uncaught `QuotaExceededError` (see `docs/journal/2026-02-16-storage-quota-guard.md`).
- [ ] Verify web render isolation optimization reduces unnecessary re-renders for `AgentsPanel`/`OutputHeader`/`OutputBody` while typing in input dock, and confirm ACP interactions still behave identically (see `docs/journal/2026-02-16-web-render-isolation-memoization.md`).
- [ ] Verify ACP debug runtime metrics refresh cache hit/miss counters when conversation rendered window changes (see `docs/journal/2026-02-16-web-render-isolation-memoization.md`).
- [ ] Verify login shell E2E remains stable after render-isolation hook ordering fix (no blank page / no TDZ during initial App render) (see `docs/journal/2026-02-16-web-render-isolation-memoization.md`).
- [ ] Verify `Connection unavailable (gateway response). Reconnecting...` is hidden from `ErrorBanner` and only reflected by connection badge state transitions (`connecting`/`reconnecting`) (see `docs/journal/2026-02-16-connection-unavailable-banner-filter.md`).
- [x] Verify `scripts/check_team_proto_codegen.sh` fails fast when `team.proto` generated symbols drift and when generated protobuf Rust files are accidentally tracked (see `docs/journal/2026-02-15-team-proto-codegen-check.md`).
- [ ] Verify new `userdocs` information architecture and cross-page navigation links after content expansion (see `docs/journal/2026-02-15-userdocs-content-expansion.md`).
- [ ] Verify newly added `userdocs` practical pages (`configuration`, `review`, `security`, `daily checklist`) align with current UI labels and runtime behavior (see `docs/journal/2026-02-15-userdocs-content-expansion.md`).
- [ ] Verify `userdocs` build no longer reports broken `/` links and deprecation warnings after root-slug/config migration (see `docs/journal/2026-02-15-userdocs-build-root-link-fix.md`).
- [x] Verify Docusaurus userdocs site install/build flow in CI (see `docs/journal/2026-02-15-userdocs-ci-build-check.md`).
- [x] Verify tool call fold behavior auto-collapses after live status ends while still allowing manual expand/collapse (see `docs/journal/2026-02-14-acp-search-fold-plan-history.md`).
- [x] Add virtualized ACP conversation window for non-stick mode to cap DOM render size on long sessions (see `docs/journal/2026-02-14-acp-search-fold-plan-history.md`).
- [x] Memoize ACP conversation row rendering (`ConversationBubble` + tool/plan/markdown bubbles) to cut repeated render work during scroll (see `docs/journal/2026-02-14-acp-search-fold-plan-history.md`).
- [x] Guard ACP conversation virtual-slice/freeze state against blank-window rendering when stale viewport/freeze state occurs; recover without requiring manual `Jump to bottom` (see `docs/journal/2026-02-14-acp-conversation-blank-window-guard.md`).
- [x] Add IME-safe keyboard guards for input history recall (`nativeEvent.isComposing`/keyCode `229`) with unit coverage (see `docs/journal/2026-02-14-acp-search-fold-plan-history.md`).
- [x] Add unit-level verification for reusable `requestAnimationFrame` throttle dedupe/cancel behavior in ACP conversation scroll path (see `docs/journal/2026-02-14-acp-search-fold-plan-history.md`).
- [x] Close input history menu on typing, arrow-history navigation, `Escape`, and outside click for keyboard/mouse accessibility (see `docs/journal/2026-02-14-acp-search-fold-plan-history.md`).
- [x] Add input history privacy guard to avoid persisting obvious secret-like commands in localStorage (see `docs/journal/2026-02-14-acp-search-fold-plan-history.md`).
- [x] Move ACP `Interrupt` control into input dock top row and colocate with `History` actions (see `docs/journal/2026-02-14-input-dock-interrupt-relocation.md`).
- [x] Increase unit coverage for input dock interaction branches and ACP tab interaction callbacks (`input_dock.tsx`, `acp_panel.tsx`) (see `docs/journal/2026-02-14-input-dock-interrupt-relocation.md`).
- [x] Increase unit coverage for `use_acp_conversation` by extracting deterministic helper decisions into pure functions and testing branch-heavy paths (see `docs/journal/2026-02-14-use-acp-conversation-coverage.md`).
- [x] Refactor `InputDock` into stable flow rows (`input-row` + `input-editor-row`) and remove absolute-position action overlay to prevent tablet overlap (see `docs/journal/2026-02-14-input-dock-tablet-overlay-fix.md`).
- [x] Keep input editor and `Send` in one row and enlarge `Send` tap target for touch-friendly interaction (see `docs/journal/2026-02-14-input-dock-tablet-overlay-fix.md`).
- [x] Left-align `Interrupt` and `History` chips in input action row for stable control placement across breakpoints (see `docs/journal/2026-02-14-input-dock-tablet-overlay-fix.md`).
- [x] Verify input dock command history menu close hooks and arrow-key recall guards for desktop/mobile IME via unit tests (`input_dock_keyboard.test.ts`) (see `docs/journal/2026-02-14-acp-search-fold-plan-history.md`).
- [x] Verify input dock `Interrupt + History` row tap ergonomics on mobile viewport via Playwright system-Chrome assertions (`input_dock_layout.e2e.ts`) (see `docs/journal/2026-02-14-input-dock-tablet-overlay-fix.md`).
- [x] Verify iPad portrait/landscape viewports no longer show `Interrupt` overlapping textarea and action chips keep stable tap targets via Playwright (`input_dock_layout.e2e.ts`) (see `docs/journal/2026-02-14-input-dock-tablet-overlay-fix.md`).
- [x] Verify one-row `input + Send` layout contract and touch target sizing (`input_dock_render.test.tsx`, `tests/web_assets.rs`) across tablet/mobile breakpoints (see `docs/journal/2026-02-14-input-dock-tablet-overlay-fix.md`).
- [x] Keep `jump-bottom` outside `input-editor-row` to avoid fixed-position element conflicting with editor grid flow (see `docs/journal/2026-02-14-input-dock-tablet-overlay-fix.md`).
- [x] Replace `input-send-button` sizing `!important` overrides with higher-specificity selectors for maintainable mobile/desktop sizing (see `docs/journal/2026-02-14-input-dock-tablet-overlay-fix.md`).
- [x] Gate optional Playwright `system-chrome` project behind `PLAYWRIGHT_SYSTEM_CHROME=1` so default E2E runs stay Chromium-only and CI-safe (see `docs/journal/2026-02-10-playwright-e2e.md`).
- [ ] Verify `use_acp_conversation` helper extraction keeps scroll restoration and jump-badge behavior unchanged across long sessions (see `docs/journal/2026-02-14-use-acp-conversation-coverage.md`).
- [ ] Verify ACP conversation render caches and `requestAnimationFrame` scroll throttling improve long-session interaction latency (see `docs/journal/2026-02-14-acp-search-fold-plan-history.md`).
- [ ] Verify ACP memoized conversation rows reduce React commit count and scripting time on long-session scroll (see `docs/journal/2026-02-14-acp-search-fold-plan-history.md`).
- [ ] Verify ACP conversation stick-to-bottom remains stable under bursty tool-call/message updates and does not require manual jump-to-bottom recovery (see `docs/journal/2026-02-15-acp-conversation-stick-bottom-hardening.md`).
- [x] Verify ACP markdown typography and list/table/code rendering in conversation view with render tests + CSS guards (`acp_conversation_render.test.tsx`, `tests/web_assets.rs`) (see `docs/journal/2026-02-13-acp-ui-fold-markdown-mobile.md`).
- [ ] Verify mobile workspace alignment with safe-area insets and ACP header action wrapping (see `docs/journal/2026-02-13-acp-ui-fold-markdown-mobile.md`).
- [x] Compact ACP top tabs (`Conversation`/`Debug`) and debug sub-tabs on mobile breakpoints to reduce oversized tab cards (see `docs/journal/2026-02-14-acp-mobile-tab-compact.md`).
- [x] Switch ACP mobile tab sizing from fixed pixel values to fluid adaptive `clamp(...)` sizing across narrow viewport widths (see `docs/journal/2026-02-14-acp-mobile-tab-compact.md`).
- [ ] Verify ACP compact mobile tab sizing and tap comfort on real iOS/Android devices (see `docs/journal/2026-02-14-acp-mobile-tab-compact.md`).
- [ ] Verify tool call terminal output keeps ANSI styling and escapes raw HTML payloads after review hardening (see `docs/journal/2026-02-13-acp-ui-fold-markdown-mobile.md`).
- [x] Migrate Bazel from shell-wrapper targets to native `rules_rust` targets for core Rust crates (see `docs/journal/2026-02-15-bazel-native-rules-rust-core.md`).
- [ ] Verify Bazel CI workflow and local `bazel build //...` + `bazel test //...` behavior on clean environments (see `docs/journal/2026-02-15-bazel-native-rules-rust-core.md`).
- [x] Converge Bazel `crate_universe` manifest source to `Cargo.toml` and remove duplicated `Cargo.bazel.toml` to avoid Cargo/Bazel dependency drift (see `docs/journal/2026-02-16-cargo-bazel-manifest-convergence.md`).
- [ ] Verify Bazel CI cache restore/save behavior (`bazelisk-cache`, `repository-cache`, `disk-cache`) improves repeated run latency without remote cache secrets (see `docs/journal/2026-02-16-bazel-ci-cache-without-remote.md`).
- [ ] Extend native Bazel coverage to web build/test targets (replace remaining non-native gaps) (see `docs/journal/2026-02-15-bazel-native-rules-rust-core.md`).
- [x] Split Rust/Bazel CI responsibility: Rust workflow runs Cargo check + cargo-llvm-cov and Bazel workflow keeps Bazel build/test; upload Rust coverage with `rust-cargo` Codecov flag (see `docs/journal/2026-02-16-ci-rust-cargo-and-bazel-split.md`).
- [x] Verify Cargo-generated Rust lcov report (`rust-cargo.lcov`) remains stable on CI across PR and main branch runs (see `docs/journal/2026-02-16-ci-rust-cargo-and-bazel-split.md`).
- [ ] Verify ACP tool call Input/Output sections render structured key-value payload views (not raw JSON-first) across Codex/Gemini/Kimi sessions (see `docs/journal/2026-02-15-acp-tool-call-humanized-rendering.md`).
- [ ] Verify lazy payload rendering keeps tool call fold open/close interaction smooth under large JSON payloads and terminal streams (see `docs/journal/2026-02-15-acp-conversation-runtime-metrics-and-segmented-rendering.md`).
- [ ] Verify ACP Debug Runtime metrics reflect conversation virtualization/cache hit behavior during long runs (see `docs/journal/2026-02-15-acp-conversation-runtime-metrics-and-segmented-rendering.md`).
- [x] Verify Rust Codecov upload appears with the `rust-cargo` flag on push and pull request runs (see `docs/journal/2026-02-16-ci-rust-cargo-and-bazel-split.md`).
- [x] Verify Web Codecov upload appears with the `web` flag on push and pull request runs (see `docs/journal/2026-02-13-codecov-web-coverage.md`).
- [x] Verify Codecov patch coverage reflects new DB/Team/API/Actor mailbox tests from the hardening pass (see `docs/journal/2026-02-13-codecov-coverage-hardening.md`).
- [ ] Verify Gemini preset event streaming and session clearing (see `docs/journal/2026-02-10-acp-gemini-kimi.md`).
- [ ] Verify Kimi preset event streaming and session clearing (see `docs/journal/2026-02-10-acp-gemini-kimi.md`).
- [ ] Verify `acp/session/clear` defaults provider based on agent command when omitted (see `docs/journal/2026-02-10-acp-gemini-kimi.md`).
- [ ] Verify `codex_acp.default_mode` only applies to Codex and not Gemini/Kimi (see `docs/journal/2026-02-10-acp-gemini-kimi.md`).
- [ ] Verify MCP servers from `mcp.json` are injected for Codex/Gemini/Kimi ACP sessions (see `docs/journal/2026-02-12-mcp-skills-injection.md`).
- [ ] Verify skills from `skills.json` are injected into ACP prompts for Codex/Gemini/Kimi (see `docs/journal/2026-02-12-mcp-skills-injection.md`).
- [x] Verify `scripts/setup_team_skills.sh` appends Team leader/worker skill entries into `~/.agenthub/skills.json` without dropping existing skills and that new ACP sessions receive both `team-leader-orchestrator` and `team-worker-executor` blocks (see `docs/features/agents-teams.md`).
- [x] Verify Codecov uploads Rust and web coverage artifacts (see `docs/journal/2026-02-12-codecov-ci.md`).
- [x] Restore `npm ci` in workflows after updating `web/package-lock.json` (see `docs/journal/2026-02-12-codecov-ci.md`).
- [x] Verify A2A Agent Team phase-1 APIs and event ordering (see `docs/journal/2026-02-12-a2a-agent-team-phase1.md`).
- [x] Verify Team Workbench UI (`/teams`) create/run/step/message interaction flow end-to-end in a real browser (see `docs/journal/2026-02-16-team-workbench-ui.md`).
- [x] Verify Team creation quest modal (`Create Team` dialog stages: Mission Brief -> Leader Forge -> Recruit Workers -> Launch Team) works on desktop/mobile, including leader-only flow, manual JSON override path, and Team-Forge-only member selection with workdir visibility (see `docs/journal/2026-02-16-team-create-quest-modal.md`).
- [x] Verify Team creation modal leader/worker model selectors stay in sync with Agent preset options (`Codex ACP`/`Gemini CLI`/`Kimi CLI`) and no longer require manual model text input (see `docs/journal/2026-02-16-team-create-quest-modal.md`).
- [x] Verify Team create flow supports structured leader/worker config (`leader_member_id`, per-member `model`/`prompt`/`skills`) with generated default workflow steps (`leader_plan` -> `worker_*` -> `leader_synthesize`) and remains compatible with run bootstrap/orchestrator dispatch (see `docs/journal/2026-02-16-team-snapshot-member-console.md`).
- [x] Verify Playwright Team UI E2E (`web/tests/e2e/team_page.e2e.ts`) stays green in CI and covers staged Team Forge creation with mocked API responses for leader/worker model selection and generated workflow steps (see `docs/journal/2026-02-16-team-ui-e2e-playwright.md`).
- [x] Verify Team Forge stage guardrails (`Next Stage` blocking reason, stage jump lock, duplicate member prevention, `Resolve Duplicates`, `Auto Fill Party`) on desktop/mobile to ensure quest flow remains smooth (see `docs/journal/2026-02-16-team-forge-quest-flow-ux-guardrails.md`).
- [x] Verify Team run snapshot API (`GET /api/teams/runs/:run_id/snapshot`) and `/teams` Overview/Mailbox/Member Console tabs stay consistent under active run updates (see `docs/journal/2026-02-16-team-snapshot-member-console.md`).
- [x] Verify Team run list API (`GET /api/teams/:id/runs`) pagination/filter behavior with high-volume automated checks (120 runs + multi-page status filtering) (see `docs/journal/2026-02-16-team-runs-list-api.md`).
- [x] Verify Team run list UI status filter + `Load More` paging controls keep active run selection stable while switching filters/teams (see `docs/journal/2026-02-17-team-run-list-filter-pagination-controls.md`).
- [x] Verify Team run list uses server cursor (`before_created_at`) instead of offset-style paging and keeps per-team filter memory when switching teams (see `docs/journal/2026-02-17-team-run-list-cursor-and-per-team-filter-memory.md`).
- [x] Verify Team output preview policy: before selecting a specific member, `/teams` `Events` and `Member Console` show only latest 5 run records; selecting a member restores full member history (see `docs/journal/2026-02-17-team-output-preview-limit.md`).
- [x] Verify Team mailbox template workflow (`leader_task_assignment` / `clarification_request` / `worker_status` / `profile_patch_proposal`) can be applied from UI and remains aligned with backend default leader/worker prompts (see `docs/journal/2026-02-17-team-mailbox-structured-templates.md`).
- [x] Verify extracted `TeamMailboxPanel` component preserves IM conversation behavior (member focus, unread, auto-follow, advanced controls) in Playwright (see `docs/journal/2026-02-18-team-mailbox-panel-component-extraction.md`).
- [x] Verify extracted `TeamMemberConsolePanel` component preserves preview/member-history refresh behavior in Team Workbench (see `docs/journal/2026-02-18-team-member-console-panel-component-extraction.md`).
- [x] Verify extracted `TeamSidebar` component preserves Team list selection and Team Forge launch behavior in full Team Playwright flow (see `docs/journal/2026-02-19-team-sidebar-component-extraction.md`).
- [x] Verify extracted `TeamRunPanel` component preserves run filter/paging/selection and create-load controls in full Team Playwright flow (see `docs/journal/2026-02-19-team-run-panel-component-extraction.md`).
- [x] Verify extracted `TeamOverviewPanel` component preserves snapshot refresh/member->mailbox jump behavior in Team Playwright flow (see `docs/journal/2026-02-19-team-overview-events-panel-component-extraction.md`).
- [x] Verify extracted `TeamEventsPanel` component preserves event auto-refresh toggle and load-older behavior in Team Playwright flow (see `docs/journal/2026-02-19-team-overview-events-panel-component-extraction.md`).
- [x] Verify extracted `TeamStepsPanel` component preserves step submit/action lifecycle controls in Team Playwright flow (see `docs/journal/2026-02-19-team-steps-panel-component-extraction.md`).
- [x] Verify Team page UI reducer phase-1 (`tab`/`runLookupId`/`eventsAutoRefresh`) keeps Team flows stable in unit + Playwright checks (see `docs/journal/2026-02-19-team-ui-state-reducer-phase1.md`).
- [x] Verify Team page UI reducer phase-2 (`runContextId`/`runInput` + step form/action controls) keeps Team flows stable in unit + Playwright checks (see `docs/journal/2026-02-19-team-ui-state-reducer-phase2-run-step.md`).
- [x] Verify Team page UI reducer phase-3 (mailbox/member conversation state: inbox filters, chat draft, unread markers, selected member) keeps Team flows stable in unit + Playwright checks (see `docs/journal/2026-02-19-team-ui-state-reducer-phase3-mailbox-member.md`).
- [x] Verify Team page UI reducer phase-4 (create-team wizard + agent forge form state) keeps Team create/run flows stable in unit + Playwright checks (see `docs/journal/2026-02-19-team-ui-state-reducer-phase4-create-team.md`).
- [x] Verify Team delete flow (`DELETE /api/teams/:id` + `/teams` `Delete Team` action) removes team-owned runs/events/steps/mailbox records and keeps next-team selection stable in desktop/mobile browsers (see `docs/journal/2026-02-17-team-delete-api-ui.md`).
- [ ] Verify OpenAPI discovery endpoints (`/api/openapi.json`, `/api/openapi/docs`) auth behavior and docs-page token fetch flow in a real browser (see `docs/journal/2026-02-16-openapi-discovery-endpoints.md`).
- [x] Refactor `web/src/pages/team_page.tsx` into smaller feature-focused components (`TeamSidebar`, `RunPanel`, tab subviews) and a reducer-based state model for maintainability (phase-1: `TeamSidebar` + `TeamRunPanel` + `TeamOverviewPanel` + `TeamEventsPanel` + `TeamMailboxPanel` + `TeamMemberConsolePanel` + `TeamStepsPanel` extracted; reducer phase-1/2/3/4 now cover tab/run filters, run/step controls, mailbox/member conversation state, and create-team wizard/forge state) (see `docs/journal/2026-02-16-team-workbench-ui.md`, `docs/journal/2026-02-18-team-mailbox-panel-component-extraction.md`, `docs/journal/2026-02-18-team-member-console-panel-component-extraction.md`, `docs/journal/2026-02-19-team-sidebar-component-extraction.md`, `docs/journal/2026-02-19-team-run-panel-component-extraction.md`, `docs/journal/2026-02-19-team-overview-events-panel-component-extraction.md`, `docs/journal/2026-02-19-team-steps-panel-component-extraction.md`, `docs/journal/2026-02-19-team-ui-state-reducer-phase1.md`, `docs/journal/2026-02-19-team-ui-state-reducer-phase2-run-step.md`, `docs/journal/2026-02-19-team-ui-state-reducer-phase3-mailbox-member.md`, `docs/journal/2026-02-19-team-ui-state-reducer-phase4-create-team.md`).
- [ ] Refactor `src/api/openapi/spec.rs` OpenAPI spec assembly into smaller helpers (`components`, `paths`) or a typed generator approach to reduce single-function complexity (see `docs/journal/2026-02-16-openapi-discovery-endpoints.md`).
- [x] Add router-level HTTP tests for Team APIs to validate wire payload/status through Axum stack (see `docs/journal/2026-02-13-a2a-team-phase1-api-hardening.md`).
- [x] Add Team spec schema validation and API tests for invalid spec payloads before orchestrator wiring.
- [x] Define cross-field Team spec constraints (entrypoint/member/step graph) for scheduler phase.
- [x] Add Team spec compatibility versioning (`spec_version`) and migration checks before scheduler rollout.
- [x] Implement team step lifecycle persistence and event emission (`submitted` -> `working` -> terminal states) for scheduler bootstrap.
- [x] Add scheduler-facing run status convergence rules (`working` -> `completed`/`failed`) based on step graph outcomes (see `docs/journal/2026-02-13-a2a-team-run-status-convergence.md`).
- [x] Expose scheduler-facing team step lifecycle API/service bridge for orchestrator integration (see `docs/journal/2026-02-13-a2a-team-step-lifecycle-bridge.md`).
- [x] Add scheduler-facing `input_required` / `resume` step transitions for human-in-the-loop coordination (see `docs/journal/2026-02-13-a2a-team-input-required-resume.md`).
- [x] Add actor-model mailbox APIs for Team runs (`send`/`inbox`/`ack`) with local and remote transport contracts (see `docs/journal/2026-02-13-a2a-team-actor-mailbox.md`).
- [x] Split actor message domain types into `crates/agenthub-team-actor` for local/remote transport reuse (see `docs/journal/2026-02-13-a2a-team-actor-crate-split.md`).
- [x] Verify ACP startup injects actor runtime skill and actor CLI env wiring when `/api/agents/:id/start` includes `actor_runtime` context payload (see `docs/journal/2026-02-15-a2a-team-actor-runtime-start-and-relay-replay-guidance.md`).
- [x] Replace env-based actor runtime context with scheduler-provided per-run/per-step actor context (see `docs/journal/2026-02-14-a2a-team-actor-runtime-context-start-api.md`).
- [x] Harden `actor_runtime.actor_cli_path` with strict default-binary allow-list validation and shared normalization helpers across API/agent/orchestrator (see `docs/journal/2026-02-15-a2a-team-review-hardening.md`).
- [x] Prevent actor-context starts from silently reusing already-running sessions; return conflict semantics for HTTP start requests (see `docs/journal/2026-02-15-a2a-team-review-hardening.md`).
- [x] Treat orchestrator dispatch as failed when `start_step` does not enter `working`, with best-effort member stop cleanup (see `docs/journal/2026-02-15-a2a-team-review-hardening.md`).
- [x] Validate `spec.steps` dependencies and reject cycles/missing step references before orchestrator bootstrap submits tasks (see `docs/journal/2026-02-15-a2a-team-review-followup-orchestrator-relay-hardening.md`).
- [x] Improve orchestrator dispatch loop to refresh step state after successful dispatch passes and avoid stale in-tick scheduling decisions (see `docs/journal/2026-02-15-a2a-team-review-followup-orchestrator-relay-hardening.md`).
- [x] Refresh and short-circuit per-run dispatch after failed dispatch attempts to avoid stale-step retries in the same worker tick (see `docs/journal/2026-02-15-a2a-team-review-followup-orchestrator-relay-hardening.md`).
- [x] Add idle-tick heartbeat logging for orchestrator worker to improve operational liveness visibility without noisy per-tick logs (see `docs/journal/2026-02-15-a2a-team-review-followup-orchestrator-relay-hardening.md`).
- [x] Return `JoinHandle` from orchestrator worker spawn path so caller can wire lifecycle management and panic observation (see `docs/journal/2026-02-15-a2a-team-review-followup-orchestrator-relay-hardening.md`).
- [x] Harden relay route header parsing with strict header name/value validation and reject CR/LF control characters in values (see `docs/journal/2026-02-15-a2a-team-review-followup-orchestrator-relay-hardening.md`).
- [x] Add relay timeout default/clamping and explicit HTTP client transport limits for remote mailbox delivery (see `docs/journal/2026-02-15-a2a-team-review-followup-orchestrator-relay-hardening.md`).
- [x] Reuse remote relay adapter (`OnceLock`) across worker ticks to preserve HTTP connection pooling and reduce repeated client allocation (see `docs/journal/2026-02-15-a2a-team-review-followup-orchestrator-relay-hardening.md`).
- [x] Add remote relay receiver-side replay protection guidance (timestamp skew + dedupe by message id/idempotency key) and reference implementation docs (see `docs/journal/2026-02-15-a2a-team-actor-runtime-start-and-relay-replay-guidance.md`).
- [ ] Verify remote relay receiver implementation applies timestamp window + dedupe policy in staging and captures rejection metrics by reason code.
- [x] Add internal gRPC control-plane baseline (`TeamInternalControl`) with `send/inbox/ack/transition_step` RPCs and capability-token auth checks (see `docs/journal/2026-02-15-internal-grpc-zero-trust-foundation.md`).
- [x] Add internal gRPC security bootstrap with auto-generated TLS certs + auto-generated shared secret, and `internal_grpc.security.mode=disabled` for local test runs (see `docs/journal/2026-02-15-internal-grpc-zero-trust-foundation.md`).
- [x] Enforce zero-trust worker restrictions: worker token can only operate on its own `actor_id` and cannot transition steps (see `docs/journal/2026-02-15-internal-grpc-zero-trust-foundation.md`).
- [x] Add internal gRPC node bootstrap (`IssueNodeCredential`) with auto-generated bootstrap token and scoped leader/worker capability token issuance (see `docs/journal/2026-02-15-internal-grpc-zero-trust-foundation.md`).
- [x] Add internal gRPC `mtls` mode with auto-generated CA/server/client cert chain and bootstrap-friendly optional client-auth policy (see `docs/journal/2026-02-15-internal-grpc-zero-trust-foundation.md`).
- [ ] Add same-port HTTP+gRPC multiplexing support so internal gRPC can share the public listener when required by deployment.
- [ ] Integrate SPIFFE/SPIRE (or equivalent workload identity) for production-grade mTLS node identity and automated certificate rotation.
- [ ] Validate Cloudflare deployment topology for internal gRPC: edge mTLS + origin mTLS policy, and document whether strict origin-side client-cert validation needs a non-proxied private listener.
- [x] Wire orchestrator worker loop step execution to start member agents with per-step `actor_runtime` context (`run_id`, `actor_id`, optional `channel`) in backend service loop (see `docs/journal/2026-02-15-a2a-team-orchestrator-worker-agent-start.md`).
- [x] Bootstrap orchestrator worker step queue from team spec DAG when runs have no persisted steps, then dispatch ready steps by dependency order (see `docs/journal/2026-02-15-a2a-team-orchestrator-worker-agent-start.md`).
- [x] Verify orchestrator-driven member start path injects actor runtime context and supports end-to-end inbox/ack flow (see `docs/journal/2026-02-15-a2a-team-orchestrator-worker-agent-start.md`).
- [x] Reconcile orchestrator `working` steps from `agent_sessions` terminal status (`completed`/`failed`/`cancelled`/`exited`) for single-node run convergence (see `docs/journal/2026-02-15-a2a-team-orchestrator-session-reconcile.md`).
- [x] Verify `POST /api/teams/runs/:run_id/messages/send` idempotency dedupes mailbox rows and `actor_message_sent` events for orchestrator retries (see `docs/journal/2026-02-14-a2a-team-actor-idempotent-send.md`).
- [x] Verify actor CLI default send auto-generates stable idempotency keys and `--allow-duplicate` bypasses dedupe when repeated delivery is required (see `docs/journal/2026-02-14-a2a-team-actor-idempotent-send.md`).
- [x] Verify `messages/send` returns `409 conflict` when reusing the same idempotency key with a changed payload/route (see `docs/journal/2026-02-14-a2a-team-actor-idempotent-send.md`).
- [x] Verify orchestrator worker loop drives `/api/teams/runs/:run_id/steps` lifecycle bridge end-to-end with real executors (see `docs/journal/2026-02-15-a2a-team-orchestrator-http-e2e-and-input-resume.md`).
- [x] Verify orchestrator worker loop handles `/input_required` and `/resume` transitions with idempotent retries (see `docs/journal/2026-02-15-a2a-team-orchestrator-http-e2e-and-input-resume.md`).
- [x] Implement remote mailbox relay worker (`transport=remote`) with delivery retry/dead-letter policy (see `docs/journal/2026-02-13-a2a-team-remote-relay-worker.md`).
- [x] Move actor mailbox state machine from `src/team/manager.rs` into `crates/agenthub-team-actor` using trait-based store/relay ports (see `docs/journal/2026-02-13-a2a-team-actor-crate-split.md`).
- [x] Add relay port in `agenthub-team-actor` and implement remote transport worker adapter in AgentHub (see `docs/journal/2026-02-13-a2a-team-remote-relay-worker.md`).
- [x] Replace mock route endpoint relay adapter with real network delivery adapter and auth/signing policy (see `docs/journal/2026-02-15-a2a-team-remote-relay-network-adapter.md`).
- [x] Verify `team/manager` and `api/teams` file split under CI full test pipeline (`cargo test --all`) to guard module-boundary regressions (see `docs/journal/2026-02-15-module-split-full-test-verification.md`).
- [x] Split `agent/manager` into submodules and keep each file under 1000 lines for maintainability (see `docs/journal/2026-02-13-agent-manager-file-split.md`).
- [x] Verify `agent/manager` module split under CI full test pipeline (`cargo test --all`) to guard runtime boundary regressions (see `docs/journal/2026-02-15-module-split-full-test-verification.md`).
- [ ] Verify Create Agent submit guard prevents duplicate agents and shows loading state (see `docs/journal/2026-02-10-create-agent-submit-guard.md`).
- [x] Verify Create Agent modal loads `default_worktree_root` from `/api/settings/defaults` and pre-fills workdir fallback (`~/.agenthub/worktrees`) for worktree creation flows (see `docs/journal/2026-02-15-worktree-default-root.md`).
- [x] Verify `/api/settings/defaults` enforces auth and returns `default_worktree_root` via router-level tests (see `docs/journal/2026-02-15-worktree-default-root.md`).
- [x] Verify worktree-default helper logic (`runtime defaults` + `create modal prefill`) with dedicated web unit tests to keep coverage stable (see `docs/journal/2026-02-15-worktree-default-root.md`).
- [x] Verify `create_worktree` modal defaults to non-editable workdir display and exposes override only via `Customize path` (see `docs/journal/2026-02-15-worktree-default-root.md`).
- [x] Verify `use_existing` mode no longer auto-fills workdir with default root on modal open or mode switch (see `docs/journal/2026-02-15-worktree-default-root.md`).
- [x] Verify `create_worktree` agents can restart after process reboot without failing `workdir is not empty` when the target path is already a valid git worktree for the configured repo (see `docs/journal/2026-02-15-create-worktree-restart-reuse.md`).
- [x] Verify runtime default safe paths always include `~/.agenthub/worktrees` and keep configured safe path entries deduplicated (see `docs/journal/2026-02-15-safe-path-default-worktrees.md`).
- [x] Fix Rust CI test compile failure after `AppState` default root field expansion by updating `api/agents` test state builder (see `docs/journal/2026-02-15-rust-ci-appstate-default-worktree-root.md`).
- [x] Canonicalize default actor CLI path so Bazel/macOS path aliases (`/var` vs `/private/var`) do not break actor-runtime API tests (see `docs/journal/2026-02-15-bazel-actor-cli-path-canonicalization.md`).
- [x] Make local Bazel shell wrappers resolve `npm`/`cargo` on macOS and fallback to writable Cargo home so `bazel build //...` works without custom env flags (see `docs/journal/2026-02-15-bazel-shell-wrapper-local-toolchain-paths.md`).
- [x] Raise Bazel `rust_test` timeout to `long` so CI does not fail at 300s while tests are still progressing (see `docs/journal/2026-02-15-bazel-shell-wrapper-local-toolchain-paths.md`).
- [x] Bump Bazel Rust toolchain to `1.93.1` to satisfy `time-macros 0.2.26` MSRV and fix CI unstable-feature compile failures (see `docs/journal/2026-02-15-bazel-rust-toolchain-time-macros-msrv.md`).
- [x] Pin Rust version to `1.93.1` across rustup files and CI workflows to keep Bazel/Cargo/CI toolchains consistent (see `docs/journal/2026-02-15-rust-version-pinning-unified.md`).
- [x] Guard `RustEmbed` web embedding behind non-debug cfg so Bazel debug/test builds do not fail when `web/dist` is absent (see `docs/journal/2026-02-15-bazel-debug-rust-embed-web-dist-guard.md`).
- [x] Fix post-merge CI compatibility: declare `agenthub-codex-acp` markdown prompt files as Bazel compile inputs and relax proto codegen check to tracked-file parity mode (see `docs/journal/2026-02-16-bazel-codex-md-and-proto-check-compat.md`).
- [x] Fix Bazel `web_assets_test` sandbox path resolution by reading `styles.css` from runfiles and declaring CSS test data explicitly (see `docs/journal/2026-02-16-bazel-web-assets-test-runfiles.md`).
- [x] Align root Bazel Rust targets to typedb-style `rust_library` + `rust_test(crate=...)` and pass `web/dist` as compile data so `RustEmbed` works in opt/test sandbox builds (see `docs/journal/2026-02-16-bazel-root-rust-library-and-rustembed-compile-data.md`).
- [x] Unify Cargo/Bazel proto generation contract around tracked generated proto file and re-enable Rust CI `Verify internal protobuf codegen` step (see `docs/journal/2026-02-16-proto-codegen-cargo-bazel-convergence.md`).
- [ ] Verify restored Rust CI proto-check step (`make proto-check`) remains stable on PR/main runs and catches `team.proto` drift before merge (see `docs/journal/2026-02-16-proto-codegen-cargo-bazel-convergence.md`).
- [ ] Verify agent model tag shows model flag or provider fallback (see `docs/journal/2026-02-10-agent-model-tag.md`).
- [ ] Verify agents panel list scrolls internally without page scroll (see `docs/journal/2026-02-10-agents-panel-scroll.md`).
- [ ] Verify start handles already-running agents without stale UI state (see `docs/journal/2026-02-10-agent-start-already-running.md`).
- [ ] Verify ACP chunk stitching fixes Kimi/Gemini output ordering (see `docs/journal/2026-02-10-acp-chunk-stitching.md`).
- [ ] Verify agents rows use the compact layout again (see `docs/journal/2026-02-10-agents-row-compact.md`).
- [ ] Verify agent delete/start edge cases and ACP provider detection (see `docs/journal/2026-02-10-agent-runtime-robustness.md`).
- [ ] Verify Mantine UI library POC styling and modal behavior (see `docs/journal/2026-02-09-mantine-ui-library.md`).
- [ ] Verify ESLint 9 upgrade and flat config lint run (see `docs/journal/2026-02-10-eslint-9-upgrade.md`).
- [ ] Verify workspace output header resume control, terminal auto-follow, and collapsed agents rail (see `docs/journal/2026-02-10-workspace-output-interaction.md`).
- [ ] Verify new workspace output/agents component tests and CSS guard updates (see `docs/journal/2026-02-10-workspace-output-tests.md`).
- [x] Verify Playwright E2E runs in CI and locally (see `docs/journal/2026-02-10-playwright-e2e.md`).
- [x] Verify CI workflows are split into Rust/Web/E2E pipelines (see `docs/journal/2026-02-10-ci-pipeline-split.md`).
- [ ] Investigate ACP output formatting for large chunked messages (e.g., shiro sessions) and fix cache retention/rendering.
- [ ] Refresh codex git dependencies and `Cargo.lock` after ACP protocol sync (see `docs/journal/2026-02-09-codex-acp-protocol-sync.md`).
- [ ] Verify ACP debug layout, interrupt gating, permission debug events, and tool output logging (see `docs/journal/2026-02-09-acp-debug-layout-fixes.md`).
- [ ] Verify ACP debug raw events auto-scrolls in the Raw tab (see `docs/journal/2026-02-10-acp-raw-scroll.md`).
- [ ] Verify single-instance agent start, ACP timeout cleanup, and SQLite WAL settings (see `docs/journal/2026-02-08-backend-stability-hardening.md`).
- [ ] Verify ACP command send timeout reports backpressure quickly (without indefinite hangs) when the ACP command queue is saturated or worker-side processing stalls (see `docs/journal/2026-02-16-acp-command-backpressure-timeout.md`).
- [ ] Verify ACP output cache isolation and UI stability (see `docs/journal/2026-02-07-acp-output-cache-and-polling.md`).
- [ ] Verify admin/join flows and focus ring styling after frontend refactor (see `docs/journal/2026-02-08-frontend-quality-refactor.md`).
- [ ] Verify agent list actions, input dock behavior, and permission modal rendering after component extraction (see `docs/journal/2026-02-08-frontend-quality-refactor.md`).
- [ ] Verify ACP panel rendering and debug tab controls after output refactor (see `docs/journal/2026-02-08-frontend-quality-refactor.md`).
- [ ] Verify ACP conversation scrolling and auto-collapse behavior after hook extraction (see `docs/journal/2026-02-08-frontend-quality-refactor.md`).
- [ ] Verify Output error boundary fallback and empty state rendering (see `docs/journal/2026-02-08-output-error-boundary.md`).
- [ ] Verify localStorage output cache restores output history after reload (see `docs/journal/2026-02-08-output-localstorage.md`).
- [ ] Verify cache key bump invalidates older payloads cleanly after schema changes (see `docs/journal/2026-02-08-output-localstorage.md`).
- [ ] Verify output cache stays consistent under SSE + polling after ref sync update (see `docs/journal/2026-02-08-output-cache-consistency.md`).
- [ ] Verify cached output validation drops incomplete records from localStorage (see `docs/journal/2026-02-08-output-cache-consistency.md`).
- [ ] Verify session-specific output views do not merge unrelated `latest` cache entries (see `docs/journal/2026-02-08-output-cache-consistency.md`).
- [ ] Verify `:latest` event meta loading flags reset after resolving the latest session (see `docs/journal/2026-02-08-output-cache-consistency.md`).
- [ ] Verify output cache slice helper behavior and tests (see `docs/journal/2026-02-08-output-cache-slice.md`).
- [x] Verify conversation history retains loaded older messages when cache slices refresh under long output (see `docs/journal/2026-02-08-conversation-history-retention.md`).
- [ ] Verify frontend lint passes locally and in CI (see `docs/journal/2026-02-08-frontend-lint.md`).
- [ ] Verify Vite build output and chunk warning limit after dependency updates (see `docs/journal/2026-02-08-build-chunk-warning-limit.md`).
- [ ] Review new unit tests for event cursor ordering and conversation freeze logic, expand coverage if regressions appear (see `docs/journal/2026-02-08-test-coverage-update.md`).
- [ ] Verify markdown sanitization tests and agent list keyboard navigation after review fixes (see `docs/journal/2026-02-08-review-fixes.md`).
- [ ] Verify markdown-it rendering and syntax highlighting on mobile and long ACP replies (see `docs/journal/2026-02-08-markdown-renderer.md`).
- [ ] Verify markdown whitelist auto-link only converts approved bare URLs (GitHub pull URLs) and keeps non-whitelist URLs as plain text in conversation output (see `docs/journal/2026-02-15-markdown-whitelist-autolink.md`).
- [ ] Verify ACP markdown typography (line height and letter spacing) on desktop and mobile (see `docs/journal/2026-02-08-markdown-renderer.md`).
- [ ] Verify ACP details caret indicator renders for tool calls and thinking folds (see `docs/journal/2026-02-08-acp-details-caret.md`).
- [ ] Verify ACP chunk ordering metadata fixes token-level stream ordering under SSE/polling (see `docs/journal/2026-02-08-acp-chunk-ordering.md`).
- [ ] Verify ACP interrupt button appears next to Conversation tab, Debug hides input dock, and raw events list scrolls (see `docs/journal/2026-02-08-acp-debug-interrupt.md`).
- [ ] Verify ACP permission request/response/timeout events appear in Debug raw events (see `docs/journal/2026-02-09-acp-permission-events.md`).
- [ ] Verify ACP permission requests persist with AgentHub session IDs and capture ACP session IDs (see `docs/journal/2026-02-09-acp-permission-session-id.md`).
- [ ] Verify ACP permission option IDs map correctly in the UI and approvals are accepted (see `docs/journal/2026-02-09-acp-permission-option-id.md`).
- [ ] Verify ACP permission demo allows `allow_always` options after approval (see `docs/journal/2026-02-09-acp-permission-option-id.md`).
- [ ] Verify ACP default_mode config applies to new sessions and triggers permission requests when required (see `docs/journal/2026-02-09-acp-default-mode.md`).
- [ ] Verify API payload naming remains `snake_case` after new endpoint additions (see `docs/journal/2026-02-09-api-naming-conventions.md`).
- [ ] Verify ACP Interrupt enables when tool calls are in progress and status badge shows `in_progress` (see `docs/journal/2026-02-09-acp-interrupt-in-progress.md`).
- [ ] Verify ACP tool output content filtering test coverage (see `docs/journal/2026-02-09-acp-tool-output-content-filter.md`).
- [ ] Verify UI refreshes agent status when input fails with `agent not running` (see `docs/journal/2026-02-09-agent-not-running-refresh.md`).
- [ ] Review raw HTML handling in markdown-it renderer and add sanitization if ACP output becomes untrusted (see `docs/journal/2026-02-08-markdown-renderer.md`).
- [ ] Validate SSE streaming output under proxy/load balancer conditions (see `docs/journal/2026-02-08-sse-streaming.md`).
- [ ] Verify SSE heartbeat events are emitted and ignored by the client (see `docs/journal/2026-02-08-sse-streaming.md`).
- [ ] Verify push notifications still work with the minimal service worker after PWA removal (see `docs/journal/2026-02-08-pwa-removal.md`).
- [x] Verify SSE auto-reconnect and polling fallback after network interruption (see `docs/journal/2026-02-08-sse-retry.md`).
- [ ] Verify polling pauses while SSE is OPEN and resumes on disconnect (see `docs/journal/2026-02-08-sse-retry.md`).
- [ ] Verify SSE headers prevent proxy buffering and caching (see `docs/journal/2026-02-08-sse-streaming.md`).
- [ ] Verify header network/SSE badge state transitions and Cloudflare HTML stream errors are normalized to compact connectivity messages (see `docs/journal/2026-02-15-sse-connection-indicator-and-error-sanitization.md`).
- [ ] Verify bounded SSE fan-in channel behavior under burst output and slow/disconnected clients, ensuring memory does not grow unbounded and polling/history catch-up remains correct (see `docs/journal/2026-02-15-sse-bounded-fan-in-and-connection-status-followups.md`).
- [ ] Verify multi-agent SSE subscription keeps a stable single connection across active-agent switches and updates agent status in background (see `docs/journal/2026-02-15-sse-multi-agent-subscription.md`).
- [ ] Verify UUIDv7 ordering and pagination across API/SSE (see `docs/journal/2026-02-08-event-seq-uuidv7.md`).
- [ ] Verify global event ordering uses `event_id` across API/SSE/polling and pagination (see `docs/journal/2026-02-08-global-event-ordering.md`).
- [ ] Verify compareEventOrder is deterministic when `event_id` or `ts` is missing (see `docs/journal/2026-02-08-global-event-ordering.md`).
- [ ] Verify gzip compression for static assets and JSON responses (see `docs/journal/2026-02-08-gzip-compression.md`).
- [ ] Verify hourly `log_path` output and stdout suppression when configured (see `docs/journal/2026-02-09-hourly-log-path.md`).
- [ ] Verify debug builds serve `web/dist` and release builds force embedded assets (see `docs/journal/2026-02-09-web-dir-debug-embedded-release.md`).
- [ ] Verify db init warns on index creation failures and unexpected ALTER errors (see `docs/journal/2026-02-08-db-init-index-logging.md`).
- [ ] Verify output ordering remains stable with mixed legacy numeric and UUIDv7 seq values (see `docs/journal/2026-02-08-output-ordering-mixed-seq.md`).
- [ ] Decide whether to serialize `event_id` as a string in the web API once IDs approach JS safe integer limits (see `docs/journal/2026-02-08-output-ordering-mixed-seq.md`).
- [ ] Verify conversation messages remain fully visible without auto-collapse (see `docs/journal/2026-02-08-conversation-message-display.md`).
- [ ] Verify frontend UUIDv7 library usage and bundle build after dependency install (see `docs/journal/2026-02-08-frontend-uuidv7-library.md`).
- [ ] Verify ACP sessions still start after crate split and events are persisted/broadcast (see `docs/journal/2026-02-12-acp-crates-split.md`).
- [ ] Verify MCP/skills injection still works after ACP crate split (see `docs/journal/2026-02-12-acp-crates-split.md`).

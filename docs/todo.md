# TODO

Active backlog only. Keep this file small and current.

## Release And Packaging

- [ ] `P1` Verify preview release partial-asset behavior: semver release run `29194967848` for `v0.0.11` proves Linux `x86_64` / `aarch64` release builds used `release-vendored-openssl`, avoided the stale cross-sysroot OpenSSL panic, and published canonical `agenthub` / `agenthub-acp` assets. A 2026-07-19 13:44 UTC workflow audit found no newer release evidence; historical failed run `25605747548` for `v0.0.3` uploaded workflow artifacts but skipped `Create Release`, and release `v0.0.3` has no assets, so it cannot prove partial release publication. Local guard coverage now rejects release workflow drift that would re-enable matrix `fail-fast`, remove the `Create Release` `always()` path, require a fully successful build matrix before collecting artifacts, stop downloading merged `release-*` artifacts, drop the zero-binary fail-closed check, or remove the partial-build warning. Remaining evidence is still a preview release run showing successful binary assets publish when one release matrix target fails. Record the preview workflow run ID and release URL in [journal/2026-04-20-release-vendored-openssl-and-partial-assets.md](journal/2026-04-20-release-vendored-openssl-and-partial-assets.md).
- [ ] `P1` Verify the trimmed `Release Prebuild` workflow after the `agenthub-codex-acp` release artifact removal lands: run `29639782865` proves `push` to `main` still exercises the release-target matrix and packages only `agenthub` / `agenthub-acp` archives, but logs still show `agenthub-codex-acp v0.10.0` compiling through the old ACP adapter dependency path. A 2026-07-19 local split moved the Codex provider dependency to package `agenthub-codex-acp-runtime` while retaining the compatibility binary target `agenthub-codex-acp`; a 2026-07-19 13:44 UTC external workflow audit found no newer `Release Prebuild` run after `29639782865`. Remaining evidence is a release/prebuild run whose actual build scope no longer includes the legacy package, while still catching Linux release-only cross regressions. Notes: [journal/2026-04-20-release-vendored-openssl-and-partial-assets.md](journal/2026-04-20-release-vendored-openssl-and-partial-assets.md).

## Team Workspace Browser Matrix

Stable contracts:

- [features/frontend-design.md](features/frontend-design.md)
- [features/workspace-unified-ia.md](features/workspace-unified-ia.md)
- [features/team-channels-threads.md](features/team-channels-threads.md)
- [features/agents-teams.md](features/agents-teams.md)
- [features/teams-collaboration-playbook.md](features/teams-collaboration-playbook.md)

Matrix to keep current on `agenthub.hawkingrei.com` and in PR browser evidence:

- [ ] `P2` PWA installability: local app-router coverage now proves shell fallbacks, `sw.js`, and `manifest.webmanifest` stay `no-cache`, hashed `/assets/*` stay immutable, and missing asset paths do not fall back to HTML. 2026-07-19 production-domain retries at 07:37 UTC, 09:47 UTC, 11:15 UTC, 12:43 UTC, 13:04 UTC, 13:23 UTC, and 13:44 UTC for `/workspace/teams`, `/sw.js`, `/manifest.webmanifest`, and a missing `/assets/*` probe still returned Cloudflare `502`, which only proves the deployed entrypoint was unavailable. Remaining work is deployed-domain verification that manifest/service-worker remain installable without stale shell caching and that CDN/proxy headers preserve the same contract after the entrypoint is healthy. Stable contract: [features/web-static-assets-and-pwa.md](features/web-static-assets-and-pwa.md); notes: [journal/2026-04-03-pwa-install-and-team-permission-card-collapse.md](journal/2026-04-03-pwa-install-and-team-permission-card-collapse.md), [journal/2026-07-19-pwa-cache-control-router-guard.md](journal/2026-07-19-pwa-cache-control-router-guard.md).

## Team Workspace Architecture

- [ ] `P1` Phase 1/2 unified workspace shell follow-up: continue deeper shell reuse without collapsing Team task-first semantics. Current local route-facade work has moved Team route parsing/building/types, route-selection snapshots, active-lens fallback, sidebar subject-pane resolution, named canonical Team subpath helpers, channel-scoped member profile panel routes, deprecated `search` compatibility, compatibility-only legacy query construction, and Team E2E navigation entrypoints into `web/src/pages/team/team_route_helpers.ts`. The boundary guard now rejects direct global Team route symbol imports outside the facade and no longer allowlists `TeamPage` or `AgentNodesWorkbench`; direct Team navigation scanning covers Team page modules, node workbench, shell lens entrypoints, and Team E2E helpers; production-source link scanning rejects literal Team `href` / `to` links outside named facade builders; production sources cannot import the positional `buildCanonicalTeamSubpath` helper instead of named Team route builders; and the Team workspace header, selected-team/body stack wrappers, plus channel/thread split layout now reuse shared `WorkspaceSectionShell` / `WorkspaceContentStack` / `WorkspaceSplitPaneLayout` chrome with explicit compact agent-workspace coverage. Remaining work is deeper shell reuse beyond those wrapper primitives and browser evidence across deployed Team navigation surfaces. Stable contract: [features/workspace-unified-ia.md](features/workspace-unified-ia.md); evidence: [journal/2026-04-18-workspace-shell-route-phase1.md](journal/2026-04-18-workspace-shell-route-phase1.md) and [journal/2026-04-18-workspace-shell-phases-1-3-convergence.md](journal/2026-04-18-workspace-shell-phases-1-3-convergence.md).
- [ ] `P2` Frontend performance hardening for Team and ACP-heavy pages: collect deployed authenticated Team/ACP-heavy browser traces and profiler evidence before making broad page-level responsiveness claims. Local evidence already covers row-local rerender guards, Team workspace context churn reduction, bounded recent-tail rendering for ACP/Team long histories, and authenticated Playwright long-history checks for Team channel, Team mailbox, and Team-member ACP open paths in both Vite dev mode and a local production bundle served by `vite preview`. Stable contracts: [features/frontend-design.md](features/frontend-design.md) and [features/acp-runtime.md](features/acp-runtime.md); evidence: [journal/2026-07-19-acp-conversation-long-history-guard.md](journal/2026-07-19-acp-conversation-long-history-guard.md), [journal/2026-07-19-acp-conversation-row-rerender-guard.md](journal/2026-07-19-acp-conversation-row-rerender-guard.md), [journal/2026-07-19-team-thread-row-rerender-guard.md](journal/2026-07-19-team-thread-row-rerender-guard.md), [journal/2026-07-19-team-thread-reply-window.md](journal/2026-07-19-team-thread-reply-window.md), [journal/2026-07-19-team-channel-activity-row-rerender-guard.md](journal/2026-07-19-team-channel-activity-row-rerender-guard.md), [journal/2026-07-19-team-channel-conversation-tail-window.md](journal/2026-07-19-team-channel-conversation-tail-window.md), [journal/2026-07-19-team-mailbox-conversation-row-rerender-guard.md](journal/2026-07-19-team-mailbox-conversation-row-rerender-guard.md), [journal/2026-07-19-team-mailbox-conversation-tail-window.md](journal/2026-07-19-team-mailbox-conversation-tail-window.md), [journal/2026-07-19-team-task-board-card-rerender-guard.md](journal/2026-07-19-team-task-board-card-rerender-guard.md), [journal/2026-07-19-team-workspace-context-rerender-split.md](journal/2026-07-19-team-workspace-context-rerender-split.md), [journal/2026-07-19-frontend-performance-browser-baseline.md](journal/2026-07-19-frontend-performance-browser-baseline.md).

## Team Runtime And Task Model

Stable contracts:

- [features/agents-teams.md](features/agents-teams.md)
- [features/team-execution-vocabulary.md](features/team-execution-vocabulary.md)
- [features/team-workspace-memory-contract.md](features/team-workspace-memory-contract.md)

- [ ] `P1` Verify remote Team direct-mailbox routing on real multi-node teams: local API regression coverage proves inferred single-member remote routing preserves mention metadata plus summary/`detail_ref`, and manager-level relay coverage now proves the remote HTTP relay body preserves the same metadata plus `message_kind`. Remaining evidence is a real multi-node rollout confirming direct single-member delivery still preserves that metadata when the recipient agent is remote and transport falls back to p2p relay. Notes: [journal/2026-03-26-team-direct-mailbox-summary-first.md](journal/2026-03-26-team-direct-mailbox-summary-first.md), [journal/2026-05-26-team-remote-direct-mailbox-routing.md](journal/2026-05-26-team-remote-direct-mailbox-routing.md).

## Object Storage

- [ ] `P1` Keep `agenthub-object-store/s3` out of release feature sets until a reviewed release build intentionally includes it. PR #890 merged with `Rust (Object Store S3 MinIO)` green, and main push Rust workflow run `29639782907` / job `88068255089` passed the MinIO-backed S3 fixture; local manifest/workflow guard coverage rejects default-feature drift, explicit release S3 features, release `--all-features`, and premature TODO closure. Remaining evidence is reviewed release-build intent to enable S3. Stable contract: [features/object-storage-opendal.md](features/object-storage-opendal.md); notes: [journal/2026-07-18-object-store-s3-minio-fixture.md](journal/2026-07-18-object-store-s3-minio-fixture.md), [journal/2026-07-18-object-upload-large-object-path.md](journal/2026-07-18-object-upload-large-object-path.md).

## Maintenance Rules

- Keep only open work here. Remove completed items after evidence lands in a journal, PR, or canonical feature spec.
- Prefer canonical feature specs in [features/](features/) over stale micro-journal references whenever the contract is already stable.
- Collapse duplicated verification bullets into one umbrella matrix when they describe the same rollout surface.

# TODO

Active backlog only. Keep this file small and current.
- [ ] Verify the agents workbench lazy-split LCP improvement after merge: `agents` route should ship a small primary shell chunk while `OutputBody`/`InputDock`/ACP workbench code loads lazily, the deployed site should show a lower LCP than the previous monolithic route, and `AcpDebug` should be evaluated as a separate follow-up split if the workbench chunk still dominates. Record focused notes and push/PR CI run IDs in `docs/journal/2026-04-06-agents-lcp-workbench-split.md`.
- [ ] Verify explicit node startup boundaries after merge: `server.role = "node"` should boot internal gRPC only, skip main-only startup side effects (`ensure_root`, push/VAPID initialization), and fail fast when `server.node_id` is missing or `internal_grpc.enabled` is false. Record focused notes and push/PR CI run IDs in `docs/journal/2026-04-05-node-mode-startup-boundary.md`.
- [ ] Extend shared-thread Team message sends to honor an explicit server-side `idempotency_key` on `POST /api/teams/:team_id/tasks/:task_id/messages` so repeated submits dedupe across tabs/retries, not just inside one page session. Record follow-up notes in `docs/journal/2026-04-05-team-channel-send-feedback-and-visibility.md`.
- [ ] **Design System & Tailwind Optimization**: Migrate hardcoded hex colors (e.g., `#f7f7f5`, `#edf2ff`) from `tailwind_classes.ts` into `tailwind.config.cjs` as semantic theme tokens (e.g., `notion-bg-subtle`, `notion-bubble-user`). Reduce redundancy by using composed Tailwind classes for shared bubble patterns.
- [ ] **Frontend Architecture Refactoring**: Continue shrinking the remaining heavy ACP workbench files after the recent splits: keep decomposing `web/src/app.tsx`, `web/src/components/agents_workbench.tsx`, and the residual tool payload/rendering path so conversation/runtime wiring, payload renderers, and route-shell concerns land in smaller stable modules.
- [ ] **Framework Integration & UI Standards**: Standardize on Mantine primitives (`UnstyledButton`, `Box`, `ActionIcon`) as base components instead of raw HTML tags to leverage built-in A11y and focus management while styling with Tailwind. Bridge Mantine theme variables (spacing, radius) more deeply into the Tailwind utility layer.
- [ ] **Performance & UX Hardening**: Evaluate advanced windowing solutions like `react-virtuoso` for the ACP conversation list to ensure smooth scrolling and reliable "stick-to-bottom" behavior in extremely long histories. Implement a `ConversationContext` to manage cross-cutting UI states (e.g., "Collapse All", focus jumping) without deep prop drilling.
- [ ] Continue consolidating the web workbench onto shared UI primitives: migrate the remaining Team panels, dock/toolbars, and row/list affordances onto the new `web/src/ui/primitives.tsx` layer so repeated Tailwind strings stop drifting independently. Record focused notes and follow-up validation in `docs/journal/2026-04-04-ui-primitives-bootstrap.md`.
- [ ] Verify Team web retention caps after merge: shared-thread conversation should keep only the newest recent-20 messages in client state, replace-refresh Team run events/member events should stay bounded instead of growing without limit, and explicit older-event browsing should continue to work for `prepend` paths. Record focused notes and push/PR CI run IDs in `docs/journal/2026-04-04-team-web-retention-caps.md`.
- [ ] Verify provider-driven ACP session selectors on real non-Codex runtimes after merge: in Agent detail and Team member ACP Debug, `mode`/`model` should render as select controls when the upstream ACP provider publishes `config_options`; confirm this on at least one real Gemini ACP session (and Kimi if available), including successful `Set Model` and `Set Mode` actions without manual ID entry. Record focused notes and push/PR CI run IDs in `docs/journal/2026-04-03-team-acp-config-selectors.md`.
- [ ] Verify installable PWA behavior after merge without reintroducing stale shell caching: `agenthub.hawkingrei.com` should expose a valid manifest/service-worker pair for installability, while a fresh web deploy still takes effect on manual refresh because HTML shell routes, `sw.js`, and `manifest.webmanifest` remain `no-cache` and only `/assets/*` stays immutable. Record browser notes and push/PR CI run IDs in `docs/journal/2026-04-03-pwa-install-and-team-permission-card-collapse.md`.
- [ ] Verify the explicit Team mailbox receive flow after merge: `agenthub actor inbox` and `GET /api/teams/runs/:run_id/messages/inbox` should stay read-only with pending messages preserved, while `agenthub actor receive` should acknowledge the accepted pending items and reduce `pending_count` accordingly. Record focused notes and push/PR CI run IDs in `docs/journal/2026-04-02-actor-receive-readonly-inbox.md`.
- [ ] Refactor `agenthub-codex-acp` prompt delivery onto official `openai/codex` app-server turn semantics: replace the current `submission_id`-centric prompt state machine with ACP session to `thread/start` or `thread/resume` mapping plus `turn/start` or `turn/steer` routing, while preserving approval bridging and existing ACP history replay guarantees. Record implementation notes in `docs/journal/2026-04-02-openai-codex-baseline.md`.
- [ ] Verify split release assets on both `push` and `workflow_dispatch`: the `Release` workflow should publish separate release-mode archives for `agenthub` and `agenthub-codex-acp` on `linux-amd64`, `linux-arm64`, and `darwin-arm64`, plus `agenthub-<version>-source.tar.gz` and `SHA256SUMS.txt`. Record release run IDs and uploaded asset names in `docs/journal/2026-04-02-release-assets-workflow.md`.
- [ ] Verify Gemini ACP bootstrap after merge: the Create Agent preset should use `gemini --acp`, legacy agents configured with `--experimental-acp` should still be detected as Gemini ACP providers, and Gemini sessions without prior host-side authentication should fail with an explicit setup message instead of attempting an invisible remote Google login. Record focused notes and push/PR CI run IDs in `docs/journal/2026-04-02-gemini-acp-bootstrap-and-auth-required.md`.
- [ ] Verify direct `actor send` target validation after merge: direct mailbox sends should reject role aliases such as `leader`/`worker` and require canonical Team `spec.members[].member_id` values, while human mailbox targets (`user`, `user:<id>`) and valid member IDs continue to work. Record focused notes and push/PR CI run IDs in `docs/journal/2026-03-31-actor-send-direct-target-validation.md`.
- [ ] Verify Team leader continuity workspace reuse after restart: on `agenthub.hawkingrei.com`, create/update leader-side `AGENTS.md` and `TODO.md`, restart the same Team member runtime (not `Force New Session`), and confirm the next leader launch still sees the same files while `Force New Session` continues to clear ACP continuity only. Record Chrome DevTools notes and push/PR CI run IDs in `docs/journal/2026-04-01-runtime-launch-vs-acp-continuity-comments.md`.
- [ ] Verify explicit `agents` API validation after merge: `POST /api/agents` should reject blank `name` / `command`, unknown `worktree_mode`, and invalid `agent_loop_*` enablement payloads at the HTTP boundary, while `POST /api/agents/:id/input` should reject blank `input`, `message_id`, and `session_id` values before they reach the agent manager. Record focused notes and push/PR CI run IDs in `docs/journal/2026-03-31-agents-api-explicit-validation.md`.
- [ ] Verify the Team Bento/Neo-Minimal shell refresh after merge: the Team workbench should present clearer primary/secondary box hierarchy across sidebar, workflow tabs, conversation, and Kanban surfaces on both desktop and compact layouts, without regressing existing navigation or compact-pane switching. Record focused notes and push/PR CI run IDs in `docs/journal/2026-03-31-team-workbench-bento-shell.md`.
- [ ] Verify the pretext-backed ACP conversation virtualization after merge: long text-heavy ACP histories should keep tighter top/bottom spacer estimates with less scroll-anchor drift, while non-text/tool rows should continue to render safely via fallback heights. Record focused notes and push/PR CI run IDs in `docs/journal/2026-03-31-pretext-acp-conversation-virtualization.md`.
- [ ] Verify the staged clap root CLI refactor after merge: `agenthub --help` should render root help and exit cleanly, `agenthub doctor` should route through clap-based parsing/help, `agenthub actor ...` should continue to preserve existing execution semantics through the new root parser, and unknown top-level subcommands should now fail fast instead of silently booting the server. Record focused notes and push/PR CI run IDs in `docs/journal/2026-03-30-clap-root-cli-refactor.md`.
- [ ] Stabilize Team member runtime restart tests by replacing the current `agenthub actor` fixture with a long-lived fixture process for lifecycle assertions. Today tests such as `force_new_session_restarts_member_runtime_with_new_session_id` can fail before the actual `force_new_session` call because the seeded member command (`agenthub actor` without a subcommand) exits immediately, so `running_session_id_for_agent(...)` never stabilizes on the returned session id. Fix the shared Team test fixture under `src/api/teams/tests.rs` / `src/api/teams/tests_core.rs` so runtime lifecycle tests assert `running -> forced_restart -> new_session_id` against a deterministic long-lived process instead of a CLI entrypoint that terminates by design.
- [ ] Verify the Codex ACP multi-agent config knob after merge: with `codex_acp.multi_agent_enabled = true` (or unset), fresh AgentHub ACP sessions should expose Codex subagent tools without requiring per-user `~/.codex/config.toml` `features.multi_agent = true`; with `codex_acp.multi_agent_enabled = false`, AgentHub should stop forcing `Feature::Collab` on the ACP adapter while leaving upstream runtime-local review/depth guards intact. Record focused notes and push/PR CI run IDs in `docs/journal/2026-03-31-codex-acp-multi-agent-config-knob.md`.
- [ ] Verify Bazel Codecov upload records `AGENTHUB_CODECOV_UPLOAD_TAG=bazel` on both `push` and `pull_request`, alongside the existing `flags: bazel` metadata. Record workflow run IDs in `docs/journal/2026-03-30-bazel-codecov-env-tag.md`.
- [ ] Verify the public Team task-create contract removal after merge: `POST /api/teams/:id/tasks` should no longer be exposed on the public HTTP surface, the normal Team UI/userdocs should stay aligned with conversation-first task requests, and internal leader/runtime task creation (`agenthub actor team-task-create` / internal gRPC) should remain intact. Record focused notes and push/PR CI run IDs in `docs/journal/2026-03-29-public-team-task-create-contract.md`.
- [ ] Verify the 2026-03-28 Dependabot alert remediation after merge: `Cargo.lock` should no longer resolve vulnerable `aws-lc-sys`, `userdocs/package-lock.json` should no longer resolve vulnerable `serialize-javascript`, CI should stay green on both `push` and `pull_request`, and GitHub Dependabot alerts `#23` / `#32` should auto-dismiss on default branch. Record run IDs and dismissal evidence in `docs/journal/2026-03-28-dependabot-alert-remediation.md`.
- [ ] Verify Team runtime stale-running reconciliation after merge: if a Team member already exited without a live runtime handle, `/api/teams/:id/runtime` and internal `describe_team_context` reads should reconcile that member to `exited`/no live session instead of leaving the Team workbench stuck on `running`. Record focused validation notes and push/PR CI run IDs in `docs/journal/2026-03-28-team-runtime-stale-running-reconcile.md`.
- [ ] Verify the Bazel CI split + coverage rollout on both `push` and `pull_request`: `Bazel Build`, `Bazel Test (Root)`, `Bazel Test (Crates)`, and `Bazel Coverage` should report independently, the aggregate `Bazel Build and Test` gate should stay green only when all four succeed, and `bazel.lcov` should upload to Codecov under the `bazel` flag. Record workflow run IDs in `docs/journal/2026-03-28-bazel-ci-job-split-and-coverage.md`.
- [ ] Verify canonical Team shared-thread resolution after merge: `GET/POST /api/teams/:id/shared_thread` should keep one stable `# all` target across refreshes, duplicate legacy shared-thread tasks should resolve to the thread with the newest persisted conversation activity, and the Team page should no longer depend on `list_tasks(limit=100)` to find the shared conversation. Record focused validation notes and push/PR CI run IDs in `docs/journal/2026-03-28-team-shared-thread-canonical-target.md`.
- [ ] Verify Team mailbox priority classes and idle-first permission review routing after merge: ordinary mailbox traffic should stay `general` and wait for the unread-summary idle window, direct mailbox sends plus leader explicit mentions should stay `urgent` and nudge immediately, and `permission_review` traffic should prefer an idle reviewer (no recent non-user ACP output) before falling back to the existing non-self reviewer order. Record focused notes and push/PR CI run IDs in `docs/journal/2026-03-30-team-mailbox-priority-and-idle-review-routing.md`.
- [ ] Verify the Rust CI job split on both `push` and `pull_request`: `Rust (Cargo)` should stay as the lightweight workspace `cargo check` gate, `Rust (Proto Check)` and `Rust (gRPC Integration)` should report independently, and `Rust (Coverage)` should still upload `rust-cargo.lcov` while avoiding target-cache bloat. Record workflow run IDs in `docs/journal/2026-03-27-rust-ci-job-split.md`.
- [ ] Verify Team actor inbox snapshot consistency after merge: `actor inbox` should load `pending_count` and visible inbox rows from the same SQLite read transaction so a default unread poll cannot report `messages = []` while a newly inserted pending row is still invisible to the list query, while cursor-filtered reads may still intentionally return `messages = []` with a non-zero `pending_count`. Record focused validation notes and push/PR CI run IDs in `docs/journal/2026-03-26-actor-inbox-snapshot-consistency.md`.
- [ ] Verify deployed Pyroscope bootstrap: setting `PYROSCOPE_SERVER_ADDRESS`, `PYROSCOPE_BASIC_AUTH_USER`, and `PYROSCOPE_BASIC_AUTH_PASSWORD` on the AgentHub server should start one process-wide profiler agent, partial configuration should warn and keep the service running, and shutdown should stop the profiler cleanly. Record notes and CI run IDs in `docs/journal/2026-03-26-agenthub-pyroscope-bootstrap.md`.
- [ ] Verify the TLS dependency floor refresh: workspace resolution should keep `rustls v0.23.37`, `aws-lc-rs v1.16.2`, and `aws-lc-sys v0.39.0`, and both `cargo check` plus `bazel build //...` should stay green after the manifest version-floor update. Record notes in `docs/journal/2026-03-26-rustls-aws-lc-version-floor.md`.
- [ ] Verify Team direct-mailbox-first + summary-first communication defaults after merge: a Team conversation message with exactly one member mention and no explicit route should infer `to_member` / `to_leader` instead of broadcasting, mailbox fan-out should narrow to the single target, and `chat_message` payloads that carry `detail_ref` should preserve a concise `summary` plus normalized reference metadata. Record focused validation notes and push/PR CI run IDs in `docs/journal/2026-03-26-team-direct-mailbox-summary-first.md`.
- [ ] Verify deployed Team member agent deletion cleanup: deleting a Team member agent should prune the matching Team `spec.members[]` / `spec.steps[]` references on the backend, and the Team workbench should clear stale fallback agent names instead of leaving a deleted agent visible after refresh. Record notes and CI run IDs in `docs/journal/2026-03-24-agent-delete-team-spec-prune.md`.
- [ ] Verify deployed Team `Force New Session` recovery on `agenthub.hawkingrei.com`: when a Team member is stuck on a dirty persisted Codex session, `Teams -> Agent ACP -> Debug -> Force New Session` should clear the persisted session for that member only, restart that member into a fresh ACP session, and `Start Team` / runtime-control failures should surface member-specific conflict text instead of a generic `internal server error`. Record Chrome DevTools MCP notes plus push/PR CI run IDs in `docs/journal/2026-03-24-team-runtime-force-new-session-and-dirty-codex-resume.md`.
- [ ] Verify task-first Team model after backend orchestrator removal: creating a Team task should no longer auto-create a run, `team_tasks.assigned_member_id` should remain `NULL` unless ownership is set explicitly, and active Team docs/skills should describe runs/steps as execution/debug artifacts rather than the primary collaboration unit. Record focused validation notes and push/PR CI run IDs in `docs/journal/2026-03-23-team-task-first-without-backend-orchestrator.md`.
- [ ] Verify deployed actor mailbox hint policy: a direct `agenthub actor send --to-actor-id ...` should still proactively nudge the destination ACP session, leader-authored `channel_id=all` sends should immediately nudge only explicitly mentioned recipients, other mailbox traffic should wait for the 3-minute idle-unread window before sending a compact unread summary, and `actor inbox` should report `pending_count` on both local and p2p paths. Record visible-browser notes plus push/PR CI run IDs in `docs/journal/2026-03-22-actor-mailbox-hint-nudges.md`.
- [ ] Verify Team role index builtins after merge: leader and worker Team sessions should still expose the same `team-leader-agents-index` / `team-worker-agents-index` skill names, but the injected role index text should come from the shared Rust builder, stay slim, and continue to point at the correct role core skills. Record focused validation notes and push/PR CI run IDs in `docs/journal/2026-03-22-team-role-index-builtins.md`.
- [ ] Verify CLI-first Team actor runtime on a real Team session: leader/worker prompts should use `agenthub actor ...` commands for mailbox/team-task/time-trigger coordination, ACP runtime should no longer auto-inject `agenthub-actor-mailbox`, and Team execution should still preserve inbox-first ordering plus remote mailbox relay behavior. Record push + PR CI run IDs and any deployed notes in `docs/journal/2026-03-22-actor-runtime-cli-first.md`.
- [ ] Verify actor mailbox text-first + channel/human notification routing after merge: `actor_send --text` should preserve markdown, `actor_send --payload-json` should surface a warning recommending `text`, `actor_send --channel-id all` should persist one canonical channel message on AgentHub and broadcast mailbox fan-out while preserving `mention_actor_ids`, remote recipients should auto-route over p2p when their agent targets a remote node, and human notification sends (`to_actor_id = user` / `user:<id>`) should still land in the human mailbox path. Record focused test evidence and any deployed validation notes in `docs/journal/2026-03-22-actor-send-text-first.md`.
- [ ] Verify deployed Team permission review fallback card on `agenthub.hawkingrei.com`: after the 40-second agent-review window expires, the shared `all` channel should render a structured permission card instead of a plain text notice, inline action buttons should approve/cancel without opening a separate Permissions panel, the browser should play a short alert tone when the new human-review card first appears, and after response the card should collapse to a status-only state. Record Chrome DevTools MCP notes plus push/PR CI run IDs in `docs/journal/2026-03-26-team-permission-review-agent-first-human-tone.md`.
- [ ] Verify deployed Team UI shell refinement + route chunk split on `agenthub.hawkingrei.com`: the Team selector should behave as a much slimmer chooser, Team/ACP shells should stay thinner and wider, Team message read-state should render as a bottom-right radial indicator with hover details, and `index.html` for the deployed build should no longer preload `route-auth`, `route-teams`, or `vendor-markdown`. Record visible-browser notes plus push/PR CI run IDs after merge (see `docs/journal/2026-03-21-team-ui-shell-and-bundle-refinement.md`).
- [ ] Audit `agenthub-codex-acp` provenance and licensing metadata: if the adapter contains MIT-derived code, preserve the required upstream copyright/permission notice in a dedicated notice file or subdirectory-local license and reconcile the current `Apache-2.0` metadata before the next release.
- [ ] Verify refreshed agent-node deployment docs against a real multi-node rollout: `userdocs/docs/deployment/overview-and-topology.md`, `userdocs/docs/core/agent-nodes.md`, and `userdocs/docs/getting-started/configuration-basics.md` should match the current `internal_grpc` config shape, remote-node registration flow, and remote-target startup behavior. Record validation notes in `docs/journal/2026-03-21-agent-node-deployment-doc-refresh.md`.
- [ ] Verify distributed node phase 0/1 rollout in CI: `internal_grpc_mailbox_send_list_ack_are_wire_compatible`, `issue_node_credential_returns_phase0_metadata`, `remote_actor_grpc_pipeline_delivers_and_acks_over_tls`, `bidirectional_actor_grpc_pipeline_relays_seeded_messages_between_in_process_states`, `remote_actor_messages_relay_success_marks_message_delivered`, and `tests/distributed_p2p_pipeline.rs` should all stay green on both `push` and `pull_request`; record workflow names and run IDs in the related PR before merge (see `docs/journal/2026-03-19-distributed-node-architecture.md`).
- [ ] Verify PR `#128` Codecov patch after generated-proto ignore: `codecov/patch` should clear with `src/internal/proto/agenthub.internal.v1.rs` excluded via `codecov.yml`, while hand-written P2P/runtime files continue to drive coverage. Record the green check and report URL in `docs/journal/2026-03-21-p2p-codecov-generated-proto-ignore.md`.

## Maintenance Rules

- Keep only open work here. Remove completed items after evidence lands in a journal, PR, or canonical feature spec.
- Prefer canonical feature specs in `docs/features/` over stale micro-journal references whenever the contract is already stable.
- Collapse duplicated verification bullets into one umbrella item when they describe the same rollout surface.

## Team Model And Runtime

- [x] Improve the Team workbench UX around the task-first model: make `Conversation`, `Kanban`, `Agents`, and debug surfaces feel like one coherent workflow, reduce accidental drift back to step/run-first mental models, and tighten the operator path for common Team actions (see `docs/features/agents-teams.md`, `docs/features/frontend-design.md`, `docs/journal/2026-03-28-team-workbench-ux-alignment.md`).
- [x] Add mobile-first adaptation for Team surfaces so `Conversation`, `Kanban`, agent controls, and runtime/debug entry points remain usable on smaller screens instead of assuming desktop-only layouts (see `docs/features/agents-teams.md`, `docs/features/frontend-design.md`, `docs/journal/2026-03-28-team-workbench-ux-alignment.md`).
- [x] Strengthen Team realtime delivery beyond the current baseline: shared-thread SSE, Kanban refresh, runtime freshness, and fallback refresh behavior should stay consistent under reconnects, tab restores, and longer-running Team sessions (see `docs/features/agents-teams.md`, `docs/features/frontend-design.md`, `docs/journal/2026-03-28-team-workbench-ux-alignment.md`).
- [ ] Continue frontend performance hardening for Team and ACP-heavy pages: reduce avoidable rerenders, keep long lists and live surfaces responsive, and verify the deployed workbench remains fast under realistic Team history sizes (see `docs/features/frontend-design.md`, `docs/features/acp-runtime.md`).
- [ ] Verify the task-first Team model end to end: creating a Team task should not auto-create a run, `team_tasks.assigned_member_id` should stay `NULL` until explicit ownership, and Team docs/skills should keep `run` / `step` framed as execution and debug artifacts instead of the primary collaboration unit (see `docs/features/agents-teams.md`, `docs/journal/2026-03-23-team-task-first-without-backend-orchestrator.md`).
- [x] Close the gap between the canonical Team task contract and current implementation/UI: leader planning should own task creation and lifecycle, while human conversation remains free-form and Kanban remains the canonical task surface (see `docs/features/agents-teams.md`, `docs/journal/2026-03-19-team-task-ownership-contract.md`, `docs/journal/2026-03-28-team-workbench-ux-alignment.md`).
- [x] Finish Team runtime freshness tightening so `Start Team` / `Stop Team` update visible runtime state immediately and selected Team runtimes self-recheck while members stay active (see `docs/features/frontend-design.md`, `docs/journal/2026-03-20-team-runtime-freshness-tightening.md`, `docs/journal/2026-03-28-team-workbench-ux-alignment.md`).
- [ ] Verify the deployed Team collaboration surfaces as one joined workflow on `agenthub.hawkingrei.com`: shared-thread SSE and fallback refresh, Kanban task refresh, Agent ACP parity, workspace language cleanup, and agent lifecycle optimistic state should all hold together under normal use (see `docs/features/agents-teams.md`, `docs/features/frontend-design.md`, `docs/journal/2026-03-19-team-channel-sse-realtime.md`, `docs/journal/2026-03-20-team-channel-sse-state-and-fallback-poll.md`, `docs/journal/2026-03-20-team-agent-acp-runtime-optimistic-state.md`, `docs/journal/2026-03-20-team-workspace-language-cleanup.md`).
- [ ] Verify Team ACP permission review routing end to end: `worker -> idle peer worker when available, otherwise peer worker/leader fallback`, `leader -> subordinate worker`, no self-review, and timed-out agent review falling back to a human-visible card in `Conversation` (`all`) with inline actions plus a local alert tone (see `docs/features/agents-teams.md`, `docs/journal/2026-03-20-team-acp-permission-review-routing.md`, `docs/journal/2026-03-22-team-permission-review-human-fallback-delay.md`, `docs/journal/2026-03-26-team-permission-review-agent-first-human-tone.md`, `docs/journal/2026-03-30-team-mailbox-priority-and-idle-review-routing.md`).
- [ ] Verify Team agent self-maintenance and deferred follow-up flows: `profile_patch_proposal`, `agent_time_trigger_*`, and operator-controlled `agent_loop` should all behave consistently in Team sessions without blocking normal task progress (see `docs/features/agents-teams.md`, `docs/journal/2026-03-19-team-agent-time-triggers-and-profile-updates.md`, `docs/journal/2026-03-19-team-agent-loop-idle-followup.md`).

## Distributed Execution And Nodes

- [ ] Verify the distributed node phase 0/1 rollout in CI and a real multi-node environment: remote agent start/input/events, mailbox relay plus ack, and node-local data isolation should all hold on both `push` and `pull_request` workflows (see `docs/features/agent-nodes.md`, `docs/features/distributed-node-architecture.md`, `docs/journal/2026-03-18-agent-node-grpc-control-plane.md`, `docs/journal/2026-03-19-distributed-node-architecture.md`).
- [ ] Verify refreshed Agent Node docs against a real multi-node rollout so `configuration-basics`, `agent-nodes`, and `deployment/overview-and-topology` match the current `internal_grpc` contract and remote-node startup path (see `docs/features/agent-nodes.md`, `docs/journal/2026-03-21-agent-node-deployment-doc-refresh.md`).
- [ ] Strengthen remote-node transport posture beyond the current baseline: validate relay dedupe and timestamp-window policy in staging, design same-port HTTP plus gRPC multiplexing, and define the production identity path (`SPIFFE` / `SPIRE` or equivalent) for long-term mTLS rollout (see `docs/features/distributed-node-architecture.md`).

## ACP And Adapter Hardening

- [ ] Polish ACP-native Codex `request_user_input` UX after the inline card rollout: cover richer note-entry flows in browser-level regression fixtures, and decide whether pending questions should eventually bypass prompt-text serialization entirely (see `docs/journal/2026-04-02-openai-codex-baseline.md`).
- [ ] Verify AgentHub-managed Codex skill materialization end to end: ACP Team/runtime skills should be written under `~/.agents/skills/agenthub-runtime/...`, injected as absolute-path ACP `<skill>` wrappers, and translated by `agenthub-codex-acp` into native Codex `UserInput::Skill` items while dynamic actor runtime context remains a separate text prefix block; record evidence before closing this item (see `docs/journal/2026-03-24-codex-acp-native-skill-injection.md`).
- [ ] Audit `agenthub-codex-acp` provenance and licensing metadata so any MIT-derived material preserves the required notice and repository metadata stays legally accurate before the next release.
- [ ] Verify the codex-acp upstream PR160 sync after the current lockfile upgrade so approvals, `ModelReroute`, and model preset lookup remain compatible across config, options, and model APIs (see `docs/features/acp-runtime.md`, `docs/journal/2026-02-19-codex-acp-upstream-sync-pr160.md`).
- [ ] Establish a focused ACP real-browser regression matrix for long-session behavior: stick-to-bottom, permission history jump/copy, stale-session `send input` recovery, and debug/runtime-metrics surfaces should all remain stable under session switching and long output histories (see `docs/features/acp-runtime.md`, `docs/journal/2026-02-15-acp-conversation-stick-bottom-hardening.md`, `docs/journal/2026-02-16-acp-permission-history-jump-context.md`, `docs/journal/2026-02-16-acp-permission-history-bubble-copy.md`, `docs/journal/2026-02-18-send-input-session-guard-and-acp-type-compat.md`).

## Context, Memory, And Runtime Governance

- [ ] Finalize the Team context and memory continuity design for long-horizon memory: `L0` / `L1` / `L2` ownership, retrieval budget by prompt mode, retention and redaction, promotion rules, and pre-compaction flush behavior need one reviewed v1 contract before further implementation (see `docs/features/agents-teams.md`, `docs/journal/2026-02-22-team-context-memory-architecture.md`, `docs/journal/2026-02-22-team-memory-flush-spec.md`).
- [ ] Verify Team context and memory continuity Track 1 and Track 3 end to end, then close the umbrella milestone only after Track 2 design sign-off is in place (see `docs/journal/2026-02-22-team-context-memory-architecture.md`).
- [ ] Implement workspace-scoped context isolation fully so every agent writes only to its own `<agent_workspace>/.cache/context` tree and cross-agent sharing stays on Team channels and APIs (see `docs/features/agents-teams.md`, `docs/journal/2026-02-22-team-context-memory-architecture.md`).

## CI, Docs, And Maintainability

- [ ] Verify the Bazel CI linker baseline after installing `lld`: `bazel build //...` and `bazel test //...` on both `push` and `pull_request` should stop emitting the Rust gold-linker deprecation warning, and the workflow run IDs should be recorded before closing this item (see `docs/journal/2026-03-24-bazel-ci-lld-linker.md`).
- [ ] Verify Codecov strict-mode uploads and core CI matrices (`Rust`, `Web`, `Web E2E`, distributed node tests, Linux sandbox hardening) on both `push` and `pull_request`, and record run IDs before closing the remaining rollout items (see `docs/journal/2026-02-19-ci-libcap-linux-sandbox.md`, `docs/journal/2026-02-20-ci-codecov-fail-fast-upload.md`, `docs/journal/2026-03-19-distributed-node-architecture.md`).
- [ ] Continue `docs/features` compaction wave 2: move stable conclusions out of residual Team and UI micro-journals into canonical feature specs and replace merged records with explicit supersession pointers where useful (see `docs/features/README.md`).
- [ ] Strengthen documentation governance: every user-visible workflow change should update `userdocs/`, stable engineering contracts should live in `docs/features/`, and `docs/todo.md` should remain an active backlog rather than a historical ledger (see `docs/README.md`, `docs/journal/2026-03-24-documentation-surface-compaction.md`).
- [ ] Verify the refreshed documentation surfaces after deployment: `README.md`, `docs/README.md`, and the published site at `https://doc.agenthub.hawkingrei.com/` should stay aligned with current scripts, configuration shape, Team terminology, and user-facing navigation (see `docs/journal/2026-03-24-documentation-surface-compaction.md`).

## Summary

- add an explicit Codecov env tag to the Bazel coverage upload
- keep the existing `flags: bazel` grouping unchanged
- record the verification follow-up in `docs/todo.md`

## Validation

- `git diff --check`

## Summary

- add a focused TODO item for the flaky Team runtime restart fixture
- document the concrete failure mode in `force_new_session_restarts_member_runtime_with_new_session_id`
- capture the intended direction: replace `agenthub actor` with a deterministic long-lived fixture process for lifecycle assertions

## Testing

- `git diff --check`

[package]
name = "agenthub-auth-domain"
version = "0.0.10"
edition = "2024"
license = "Apache-2.0"

[dependencies]
anyhow = "1"
argon2 = "0.6"
rand_core = { version = "0.6", features = ["getrandom"] }
webauthn-rs = "0.5"

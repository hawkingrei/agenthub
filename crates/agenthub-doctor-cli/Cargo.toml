[package]
name = "agenthub-doctor-cli"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"

[dependencies]
agenthub-diagnostics = { path = "../agenthub-diagnostics" }
agenthub-managed-skills = { path = "../agenthub-managed-skills" }
anyhow = "1"
clap = { version = "4", features = ["derive"] }
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
serde_json = "1"

[dev-dependencies]

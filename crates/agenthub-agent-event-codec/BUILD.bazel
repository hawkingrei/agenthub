load("@crate_index//:defs.bzl", "aliases", "all_crate_deps")
load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

package(default_visibility = ["//visibility:public"])

rust_library(
    name = "agenthub_agent_event_codec",
    srcs = glob(["src/**/*.rs"]),
    crate_name = "agenthub_agent_event_codec",
    edition = "2024",
    aliases = aliases(),
    deps = all_crate_deps(normal = True) + [
        "//crates/agenthub-agent-domain:agenthub_agent_domain",
        "//crates/agenthub-db:agenthub_db",
    ],
    proc_macro_deps = all_crate_deps(proc_macro = True),
)

rust_test(
    name = "agenthub_agent_event_codec_tests",
    crate = ":agenthub_agent_event_codec",
    edition = "2024",
    aliases = aliases(
        normal_dev = True,
        proc_macro_dev = True,
    ),
    deps = all_crate_deps(normal_dev = True) + [
        "//crates/agenthub-agent-domain:agenthub_agent_domain",
        "//crates/agenthub-db:agenthub_db",
    ],
    proc_macro_deps = all_crate_deps(proc_macro_dev = True),
)
